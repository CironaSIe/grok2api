# Grok CLI / OIDC 抓包分析（对齐 sso2oauth 流程）

> **用途：** 后续升级 `~/grok2api/scripts/sso2oauth.py` 与 origin Go（`cli/oauth.go`、`web/sso_build.go`、推理 `applyHeaders`）的**行为复刻 SSOT**。  
> **原则：** 以 **真机二进制抓包 + `grok-build` 源码** 为证据；脚本顺序作骨架；**浏览器内 OIDC 同意页本抓包不可见**，不得用臆测填洞。  
> **日期：** 2026-07-22  
> **二进制：** `~/grokbuild/grok` → `grok 0.2.106 (bde89716f6)`  
> **抓包：** `~/grokbuild/grok_captures/traffic.log`（同日 15:32 起，约 233 条 HTTP）  
> **源码锚点：** `grok-build/crates/codegen/xai-grok-shell/src/auth/{device_code,config}.rs`、`xai-grok-http`  
> **脚本：** `~/grok2api/scripts/sso2oauth.py`（文内仍为 0.2.101 常量，下文对照时注明）

---

## 0. 阅读约定

### 0.1 两条不同链路（不要混）

| 链路 | 谁在用 | 本抓包是否覆盖 |
|------|--------|----------------|
| **A. 官方 CLI 登录/运行** | `grok` 二进制：device 发码 → **人在浏览器点同意** → poll token → CLI API | **CLI 侧全覆盖**；浏览器页 **不可见** |
| **B. 号池 SSO→Build（Convert）** | SSO cookie 自动 verify/approve + 同上 device/token | 脚本 `sso2oauth` 设计此链路；**本批 traffic 未抓到 accounts 自动同意** |

复刻时：

- **device/token/refresh/login-config/推理头** → 以本抓包 + 源码为准（链路 A 可见部分）。  
- **verify 页 / approve / SSO cookie** → 以 `sso2oauth.py` 既有顺序为骨架，**待专门抓 Convert 或对照脚本落盘**，不得假装本 `traffic.log` 已验证。

### 0.2 本抓包盲区（明确写出）

下列在 `traffic.log` 中 **未出现**（代理只能看到 CLI 进程出站，看不到用户浏览器渲染的 accounts 页）：

- `GET accounts.x.ai/oauth2/device?...`（用户浏览器）
- `POST auth.x.ai/oauth2/device/verify`
- `POST auth.x.ai/oauth2/device/approve`（或 consent 相关 HTML 导航）
- `GET auth.x.ai/oauth2/authorize`（PKCE 浏览器流；本批以 device 为主）
- `POST grok.com/rest/rate-limits`（脚本 00a；官方 CLI 本批未发）

### 0.3 常量（抓包 + 源码一致）

| 常量 | 值 |
|------|-----|
| `client_id` | `b1a00492-073a-47ea-816f-4c329264a828` |
| CLI UA（0.2.106） | `grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)` |
| Token 端点 | `https://auth.x.ai/oauth2/token`（**不是** `/oauth/token`） |
| Device 端点 | `https://auth.x.ai/oauth2/device/code` |
| Discovery | `https://auth.x.ai/.well-known/openid-configuration` |
| CLI proxy | `https://cli-chat-proxy.grok.com` |
| 版本指针 | `https://x.ai/cli/stable` → `0.2.106` |
| `X-XAI-Token-Auth`（API） | `xai-grok-cli` |
| Device `referrer` 表单字段 | `grok-build` |
| Device `x-grok-client-surface` | 交互完成流：`ui`（源码枚举 `Ui`/`Cli`/`Headless`） |

### 0.4 默认 OAuth2 scope（0.2.106 源码 = 本抓包 device body）

```text
openid profile email offline_access
grok-cli:access api:access
conversations:read conversations:write
workspaces:read workspaces:write
```

源码：`default_oauth2_scopes()` in `xai-grok-shell/src/auth/config.rs`。  
**相对旧抓包 / 旧 sso2oauth：** 新增 **`workspaces:read` / `workspaces:write`**。

---

## 1. 身份头矩阵（按端点类）

> 先定「类」，再挂到 sso2oauth 阶段。Go 升级应落成同一套枚举，避免再出现「全站浏览器 form」误判。

### 1.1 `CLIShared` — 共享 HTTP 客户端默认 UA

真机 `reqwest` shared client 默认：

```http
User-Agent: grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
Accept: */*
Accept-Encoding: gzip, br, deflate
```

源码渲染：`xai-grok-http` `UserAgent::render` — origin product=`grok-pager` 且 agent=`grok-shell` 且版本相同时为双 product 串。

### 1.2 `CLIAuthForm` — auth.x.ai 表单（device / token poll / 部分 refresh）

**抓包结论：使用 CLI 双 UA，不是浏览器 UA。**

| 头 | device/code | token (device_code grant) | token (refresh_token) |
|----|-------------|---------------------------|------------------------|
| `User-Agent` | 双 CLI | 双 CLI | 双 CLI |
| `Content-Type` | `application/x-www-form-urlencoded` | 同左 | 同左 |
| `Accept` | `*/*` | `*/*` | `*/*` |
| `Accept-Encoding` | `gzip, br, deflate` | 同左 | 同左 |
| `x-grok-client-version` | **有** `0.2.106` | **有** | **本批 refresh 未见** |
| `x-grok-client-surface` | **`ui`** | **`ui`** | **本批 refresh 未见** |
| `Origin` / `Referer` / `Sec-Fetch-*` | **无** | **无** | **无** |
| `X-XAI-Token-Auth` | **无** | **无** | **无**（与 API 不同） |

源码：`device_code.rs` 对 device 与 poll 均设置 `x-grok-client-version` + `x-grok-client-surface`；UA 来自 shared client。

### 1.3 `BrowserAuthHTML` — accounts 验证/同意（本抓包不可见）

仅能引用 **sso2oauth.py** 设计（Convert 用）：

- `User-Agent`: 移动/桌面 **浏览器** UA（脚本 `BROWSER_UA`）
- `Accept`: HTML
- Cookie: `sso` / `sso-rw`
- 用于：`verification_uri_complete` GET、verify POST、approve POST

**升级时：** 保持「HTML = 浏览器 / Form = CLI」双轨；**禁止**把 device/token 改成浏览器 UA（与 0.2.106 抓包冲突）。

### 1.4 `CLIApiMeta` — login-config / user / settings / models / billing

| 头 | login-config | /v1/user | /v1/settings | /v1/models | /v1/billing |
|----|--------------|----------|--------------|------------|-------------|
| UA | 双 CLI | 双 CLI | 双 CLI | 双 CLI | 双 CLI |
| `Authorization` | 无（未登录可调） | Bearer | Bearer | Bearer | Bearer |
| `x-xai-token-auth` | 无 | `xai-grok-cli` | `xai-grok-cli` | `xai-grok-cli` | `xai-grok-cli` |
| `x-grok-client-version` | 有 | 有 | 有 | 有 | 有 |
| `x-grok-client-mode` | `interactive` | `interactive` | `interactive` | `interactive` | `interactive` |
| `x-grok-client-identifier` | **`grok-shell`** | 常无 | **`grok-shell`** | 常无 | 常无 |
| `x-grok-agent-id` | 有（稳定 UUID） | 视调用 | 视调用 | 视调用 | 视调用 |
| `x-userid` / `x-email` | 无 | 部分有 | **常见** | 部分有 | 部分有 |

### 1.5 `CLIInference` — `POST /v1/responses`（运行时，非登录）

本抓包样例关键头（脱敏）：

```http
Accept: text/event-stream
Content-Type: application/json
User-Agent: grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
Authorization: Bearer <redacted>
x-xai-token-auth: xai-grok-cli
x-grok-client-version: 0.2.106
x-grok-client-mode: interactive
x-grok-client-identifier: grok-pager
x-authenticateresponse: authenticate-response
x-grok-agent-id: <uuid>
x-grok-session-id: <uuid>
x-grok-conv-id: <同 session>
x-grok-req-id: <uuid>
x-grok-turn-idx: <int>
x-grok-user-id: <uuid>
x-grok-model-override: <model>
x-compaction-at: 400000
x-compactions-remaining: 1
traceparent: 00-<trace>-<span>-01
```

注意：**推理**上 `x-grok-client-identifier` 样例为 **`grok-pager`**；**settings/login-config** 样例为 **`grok-shell`**。复刻时不要写死成单一 identifier 用于所有路径。

### 1.6 `CLIProbe` — `GET x.ai/cli/stable`

```http
Accept: */*
Accept-Encoding: gzip, br, deflate
# 无 User-Agent（本抓包）
```

---

## 2. 按 sso2oauth 阶段核对

下列阶段号与 `sso2oauth.py` 的 `phase_header` **一一对应**。  
每节：**脚本意图 → 本抓包 → 源码 → 复刻建议**。

---

### Phase 00 — SSO JWT Decode

| | |
|--|--|
| **脚本** | 解 SSO JWT；派生稳定 `agent_id`（session_id / uuid5） |
| **抓包** | 无 HTTP；CLI 侧 N/A |
| **复刻** | Convert 保留；纯 CLI device 登录可跳过 SSO |

---

### Phase 00a — Rate Limits `POST grok.com/rest/rate-limits`

| | |
|--|--|
| **脚本** | 浏览器 UA + `cookie: sso=…`，检查 `remainingQueries` |
| **抓包** | **未出现**（官方 CLI 登录序不做） |
| **复刻** | **可选**，仅 Convert 健康检查；**不要**当成 CLI 必选序。若做：明确标为 Web/SSO 探活，与 CLI 头分离 |

---

### Phase 01 — Version Check `GET https://x.ai/cli/stable`

| | |
|--|--|
| **脚本** | `Accept: */*` + encoding；记录版本正文 |
| **抓包** | **有**；头极简，**无 UA**；响应 body 版本串 / 旁路会带 CF cookie |
| **源码** | 安装/启动会读 stable 指针；运行时也可能拉取 |
| **复刻** | 建议保留为轻预检；**不要**强行塞双 CLI UA（与抓包不一致）。允许无 UA 或与 shared client 一致，二选一写死并测 |

**抓包头：**

```http
GET /cli/stable HTTP/1.1
Host: x.ai
Accept: */*
Accept-Encoding: gzip, br, deflate
```

---

### Phase 02 — Login Config `GET /v1/login-config`

| | |
|--|--|
| **脚本** | 02a **curl/7.80.0 裸**；02b full CLI 头 |
| **抓包** | **仅 full 形态**；`device_flow: true/null`；**无 curl** |
| **源码** | `remote/client.rs` 拉 login-config 判断 device_flow |
| **复刻** | **删除 curl 裸检**；只保留一次 CLI 头请求 |

**抓包头（样例）：**

```http
GET /v1/login-config HTTP/1.1
Host: cli-chat-proxy.grok.com
Accept: */*
Accept-Encoding: gzip, br, deflate
User-Agent: grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
x-grok-agent-id: <stable-uuid>
x-grok-client-identifier: grok-shell
x-grok-client-mode: interactive
x-grok-client-version: 0.2.106
```

**关联：** `device_flow` 为 false/null 时官方会禁用 device 登录；Convert 批量前可读此字段做熔断。

---

### Phase 03 — OIDC Discovery

| | |
|--|--|
| **脚本** | `Accept: */*`，几乎无 UA |
| **抓包** | **有**；**带双 CLI UA** |
| **复刻** | 用 discovery 校验 `device_authorization_endpoint` / `token_endpoint`；头对齐 CLIShared |

**抓包头：**

```http
GET /.well-known/openid-configuration HTTP/1.1
Host: auth.x.ai
Accept: */*
Accept-Encoding: gzip, br, deflate
User-Agent: grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
```

**关键 JSON 字段（抓包）：**

- `device_authorization_endpoint`: `…/oauth2/device/code`
- `token_endpoint`: `…/oauth2/token`
- `authorization_endpoint`: `…/oauth2/authorize`（浏览器 PKCE；本批 CLI 主路径未走）

---

### Phase 04 — Device Code `POST /oauth2/device/code`

| | |
|--|--|
| **脚本** | CLI UA + `surface=ui` + version；scope 到 conversations；`referrer=grok-build`；版本 0.2.101 |
| **抓包** | **一致方向**；版本 **0.2.106**；scope **含 workspaces**；曾见 **429** 后成功 |
| **源码** | `request_device_code`：version + surface + form(client_id, scope, referrer=grok-build) |
| **复刻** | **P0 对齐点** |

**抓包头：**

```http
POST /oauth2/device/code HTTP/1.1
Host: auth.x.ai
Accept: */*
Accept-Encoding: gzip, br, deflate
Content-Type: application/x-www-form-urlencoded
User-Agent: grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
x-grok-client-surface: ui
x-grok-client-version: 0.2.106
```

**抓包 body（解码后）：**

```text
client_id=b1a00492-073a-47ea-816f-4c329264a828
scope=openid profile email offline_access grok-cli:access api:access conversations:read conversations:write workspaces:read workspaces:write
referrer=grok-build
```

**响应（成功时）字段：** `device_code`, `user_code`, `verification_uri`, `verification_uri_complete`, `expires_in`≈1800, `interval`≈5  

**关联：**

- `surface=ui`：源码注释用于漏斗指标（人可完成 vs headless 污染）；Convert 自动同意仍建议 `ui` 或单独评估 `headless`（headless 表示无人完成，可能更脏）。  
- **429：** 批转换必须退避，不可狂刷 device/code。

---

### Phase 04a / 04b / 04c（前半）— Verify / Consent / Approve

| | |
|--|--|
| **脚本** | 浏览器 UA + SSO cookie；GET complete URI → verify → approve |
| **抓包** | **全部不可见** |
| **官方 CLI** | 打印 URI，**真人浏览器**完成；CLI 进程不发 verify/approve |
| **复刻** | Convert **必须**保留脚本逻辑；头用 `BrowserAuthHTML`；与 `CLIAuthForm` 严格分离。应用专用抓包或脚本 `steps/saved` 再固化字段 |

**脚本侧关联顺序（逻辑，非本 traffic 实证）：**

```text
04 device/code (CLIAuthForm)
  → 04a GET verification_uri_complete (Browser + SSO cookie)
  → 04b POST …/device/verify {user_code} (Browser)
  → 04c POST …/device/approve (Browser, 期望 302/303)
  → 04c poll token (CLIAuthForm)
```

---

### Phase 04c（后半）— Token Poll `POST /oauth2/token` (device_code)

| | |
|--|--|
| **脚本** | CLI UA + surface=ui + version；grant device_code；先 sleep 再 poll |
| **抓包** | **有**；头与脚本同族；body **无** principal；源码同样 sleep-first |
| **复刻** | 对齐；处理 `authorization_pending` / `slow_down` / `expired_token` / `access_denied` |

**抓包头：**

```http
POST /oauth2/token HTTP/1.1
Host: auth.x.ai
Accept: */*
Accept-Encoding: gzip, br, deflate
Content-Type: application/x-www-form-urlencoded
User-Agent: grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
x-grok-client-surface: ui
x-grok-client-version: 0.2.106
```

**body：**

```text
grant_type=urn:ietf:params:oauth:grant-type:device_code
device_code=<opaque>
client_id=b1a00492-073a-47ea-816f-4c329264a828
```

**成功后（脚本已做，Go 应补）：**

- 解析 access_token / id_token JWT  
- 读取 **`bot_flag_source`**（`NP` 或缺失 → 正常；其它 → 污染，打标/拒绝启用）  
- 持久化 refresh_token、expires、sub、email、team_id、client_id  

---

### Phase 04 旁路 — Refresh `POST /oauth2/token` (refresh_token)

非 sso2oauth 主路径，但是 **Go 号池热路径**，本抓包有成功样例。

**抓包头：**

```http
POST /oauth2/token HTTP/1.1
Host: auth.x.ai
Accept: */*
Accept-Encoding: gzip, br, deflate
Content-Type: application/x-www-form-urlencoded
User-Agent: grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
# 本批未见 surface/version（与 device poll 不同）
```

**body：**

```text
grant_type=refresh_token
refresh_token=<opaque>
client_id=b1a00492-073a-47ea-816f-4c329264a828
principal_type=User
principal_id=<user-uuid>
```

**复刻：**

- Go `cli/oauth.go` refresh **应带** `principal_*`（库内有 user_id 时）；无则省略以兼容旧号。  
- UA 用 CLI 双 UA；**不要**浏览器 UA；**不要**依赖 `X-XAI-Token-Auth` 做 refresh（抓包无此头）。

---

### Phase 05 — CLI Init（user / settings / models / billing / …）

| | |
|--|--|
| **脚本** | Bearer + 双 UA + mode/version + token-auth；再补 identifier / userid |
| **抓包** | **大量命中**；与脚本同族 |
| **复刻** | Convert 成功后至少 **`GET /v1/user`** 校验；全套 init 可选 |

**最小公共头：**

```http
Authorization: Bearer <access_token>
User-Agent: grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
x-grok-client-version: 0.2.106
x-grok-client-mode: interactive
x-xai-token-auth: xai-grok-cli
Accept: */*
Accept-Encoding: gzip, br, deflate
```

**差异记忆：**

- `/v1/settings`：常见 `x-grok-client-identifier: grok-shell` + `x-userid` + `x-email`  
- `/v1/user`：有时无 identifier  
- 推理 responses：identifier 样例为 **`grok-pager`**

---

### Phase 06 / 99 — Pre-flight extras / DB

| | |
|--|--|
| **脚本** | feedback/mcp 等；DB 写 `cli_bot_flag`、tags |
| **抓包** | 非认证关键；略 |
| **复刻** | Go：bot 结果进账号标签/拒绝策略；不落原始 token 到日志 |

---

## 3. 端到端关联图

### 3.1 官方 CLI（本抓包可见部分）

```text
[可选] GET x.ai/cli/stable                    (CLIProbe)
       GET auth.x.ai/.../openid-configuration (CLIShared)
       GET cli-chat-proxy/.../login-config    (CLIApiMeta)
       POST auth.x.ai/.../device/code         (CLIAuthForm, surface=ui, full scope+referrer)
         │
         ├─(人在浏览器，抓包不可见)─► accounts 同意
         │
       POST auth.x.ai/.../token  device_code  (CLIAuthForm, surface=ui)  ×N
       GET  .../v1/user|settings|models|billing
       POST .../v1/responses                  (CLIInference)
       POST .../token refresh_token           (CLI 双 UA + principal_*)
```

### 3.2 号池 Convert（sso2oauth；浏览器步待专用证据）

```text
SSO decode → [可选 rate-limits Web]
→ cli/stable → login-config(CLI) → discovery
→ device/code (CLIAuthForm)
→ verify/approve (BrowserAuthHTML + sso cookie)  ← 抓包盲区
→ token poll (CLIAuthForm)
→ 解析 bot_flag → CLI init → 落库 Build 凭证
```

---

## 4. 与当前实现的差距（升级清单）

### 4.1 `sso2oauth.py`（建议）

| 优先级 | 项 |
|--------|-----|
| P0 | `VERSION/UA` → **0.2.106** |
| P0 | scope 增加 **`workspaces:read workspaces:write`** |
| P0 | **删除** login-config `curl/7.80.0` 裸请求 |
| P1 | discovery 带双 CLI UA |
| P1 | device 429 退避 |
| P2 | 00a rate-limits 标为可选 SSO 探活 |
| P2 | 版本常量与 `x.ai/cli/stable` 可同步 |

### 4.2 origin Go（建议，相对本分析）

| 优先级 | 文件/区域 | 项 |
|--------|-----------|-----|
| P0 | `web/sso_build.go` | device/token 改 **CLIAuthForm**（双 UA + version + surface）；**不要**浏览器 form |
| P0 | 同上 | body：`referrer=grok-build`；scope 含 **workspaces** |
| P0 | 同上 + account | token 后解析 **`bot_flag_source`**，污染打标/拒绝 |
| P0 | `cli/oauth.go` | refresh：CLI UA；可选 **principal_***；scope/referrer 与 device 一致 |
| P1 | convert 预检 | login-config / stable（CLI 头，无 curl） |
| P1 | 批转换 | device **429** 退避 + bot 连续污染熔断 |
| P2 | `applyHeaders` | 区分 settings 的 `grok-shell` vs responses 的 `grok-pager`（配置化即可） |
| — | 明确不做 | 整包 PKCE；把 verify/approve 改成 CLI UA |

### 4.3 已否定的旧结论（勿再写回计划）

| 旧说 | 本证据 |
|------|--------|
| auth.x.ai 表单应全站浏览器 UA | **否** — 0.2.106 抓包 + 源码均为 CLI 双 UA |
| login-config 可用 curl 裸检 | **否** — 官方仅 CLI 头 |
| Token-Auth 必须挂在 refresh | **否** — refresh 抓包无此头；API 才有 |

---

## 5. 后续补抓建议（填盲区）

为封死 Convert 浏览器步，建议单独开 mitm 会话：

1. 仅跑 `sso2oauth.py`（或 Go Convert），代理能看到 **accounts.x.ai + auth verify/approve**。  
2. 落盘每个请求的：方法、URL、完整请求头、form body、Set-Cookie 差分。  
3. 将结果附录进本文 **§Phase 04a–04c**，替换「仅脚本设计」表述。

补抓时固定：

- 二进制或脚本 UA 版本 **0.2.106**  
- 同一 `client_id`  
- 记录 `bot_flag_source` 成功/失败各一例  

---

## 6. 快速对照卡（打印用）

```text
UA_CLI     = grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
CLIENT_ID  = b1a00492-073a-47ea-816f-4c329264a828
SCOPE      = openid profile email offline_access grok-cli:access api:access
             conversations:read conversations:write workspaces:read workspaces:write
REFERRER   = grok-build
SURFACE    = ui          # device + device-token poll
TOKEN_AUTH = xai-grok-cli  # only cli-chat-proxy API, not oauth form
MODE_DEF   = interactive   # real TUI; headless only when -p/agent headless
ID_SETTINGS= grok-shell
ID_RESP    = grok-pager     # observed on /v1/responses
REFRESH    = + principal_type=User&principal_id=<sub>
BOT_OK     = bot_flag_source in {NP, absent}
```

---

## 7. 文档关系

| 文档 | 职责 |
|------|------|
| **本文 `抓包分析.md`** | 抓包事实 + 与 sso2oauth 阶段对齐 + 升级清单 |
| `修改计划.md` | origin 工程任务 SSOT（应引用本文修正 H1 form UA 结论） |
| `sso2oauth.py` | Convert 可执行骨架；按 §4.1 升级 |
| `grok-build` 源码 | 字段语义（surface、scope、referrer） |
| `对比结果.md` §1.3 | **部分过时**（浏览器 form / curl 勘误需以本文为准） |

---

*生成说明：请求头与 body 来自 `grok_captures/traffic.log` 解析；密钥与邮箱已脱敏。浏览器 OIDC 步标注为盲区，待补抓。*
