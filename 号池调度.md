# 号池调度设计（origin）

> 产品本质：账号池调度（选号 → 调用 → 即时反馈）；OpenAI/Anthropic 只是外壳。  
> 本文固化 CLI 短时效 JWT、SSO 转换、免费/非免费分层、成功证明、通道隔离、**选号与预热共用同一优先级** 的约定。  
> OIDC/抓包细节见 `抓包分析.md` / `修改计划.md`。  
> CLI 分层与 eligibility 语义对齐并吸收 `~/grok2api` 实现（`selector._cli_layer` / `_cli_eligibility_from_ext` / `should_fill_cli_token`），在 origin 上落地为分账号 + 水位 worker。

---

## 0. 质量标尺

| 标尺 | 含义 |
|------|------|
| **完整** | 续命、复活、废弃、分层选号、分层预热、非 CLI 隔离均有定义 |
| **通用** | Web / Build / Console 通道内调度；CLI 领域逻辑可测、可配置 |
| **明确** | 谁可被选、谁可被预热、允许写哪些状态写清 |
| **无副作用** | CLI refresh/convert **不得**导致 Web/Console 行为错误或误杀免费日限号 |

### 0.1 号池特征

| 特征 | 策略默认 |
|------|----------|
| 绝大多数 **日限很紧的免费号** | 换号优先；预热 **proven 优先**，但 **非 proven 也保持一小截水位**（探测/兜底），禁止未证占满全部 warm |
| 少量 **非免费**（super/heavy 或 BuildSuper） | 优先养 CLI、优先选号（仍须成功证明） |
| 论坛/公开 SSO | RT 被外人作废 = **预期**；Convert 有界补水 |
| 成功靠换号 | 请求保延迟；修复/预热在旁路 |

### 0.2 硬约束

1. 正常 **RT 刷新 Access**；**RT 真死 → SSO Convert**；**SSO 死 → 废弃**。  
2. 允许独立线程 RT + Convert（含启动），但是 **水位预热**；且预热对象必须按 **与选号相同的分层** 选取。  
3. convert/refresh **不得**搞乱非 CLI 通道。  
4. Access ~**6h**（约 4 次 RT/天/号）；Convert ~**10–15s**，禁止当日常续命。  
5. 公用 SSO：Convert 废旧 RT；同 SSO 串行 + 世代一致。

### 0.3 相对 origin 现状：是否采纳 Python CLI 模型？

| 维度 | origin 现状（简化） | `~/grok2api` CLI | 结论 |
|------|---------------------|------------------|------|
| 免费 vs 非免费 | priority / Super / billing 有，但 **CLI 选号未硬分五层** | **硬层**：非免费∧成功 > 成功 > 信任未证 > 未知 > bot未证 | **Python 更贴号池领域** |
| 成功证明 | 弱（usage/审计，非选号主轴） | `cli_last_success` **决定层** | **应吸收** |
| 可服务性 | auth/cooldown/token 到期 | eligibility 七态与层 **正交** | **应吸收** |
| 预热 | due 全量倾向 / 无 proven 偏好 | super/heavy 积极 fill；basic 不隐式**全池** fill | **分层水位**：proven 优先养满；**非 proven 也保持少量 ready**（origin 增强，非 Python 原样） |
| 通道隔离 | Web/Build 分号 | CLI inflight/soft 不占 Web | origin 结构更优，策略对齐 Python |
| 凭证模型 | 加密 + Build 一等 | SSO 行 + ext.cli_* | origin 存储更优 |

**判定：在 CLI 调度与预热选择上，Python 的「池类型 × 成功证明 × eligibility」比 origin 当前更先进，应作为 origin 目标模型。**  
origin 保留分 Provider、加密、link、水位 worker 骨架；**选号与预热共用同一套 CLI 优先级**，而不是两套启发式。

---

## 1. 通道模型（隔离）

```text
SSO（货源）
  ├── grok_web      非 CLI
  ├── grok_build    CLI（Access+RT；分层与预热只针对此）
  └── grok_console  非 CLI
```

| 动作 | 允许影响 | 禁止影响 |
|------|----------|----------|
| RT / Convert / CLI 分层状态 | 仅 Build（及 CLI 专用计数/冷却列） | Web cookie、Web quota、Web auth、Console、Web 冷却 |
| SSO 证实死亡 | 才可废弃 Web/SSO | RT 失败升格 SSO 废弃 |
| CLI 401/403 | Build 换号 / 异步复活 / CLI soft | Web MarkFailure、整 identity 连坐 |

---

## 2. 三级水源（Build 凭证）

```text
Access 可用 → 用
Access 差 → RT refresh → 成功则写新 Access
RT 真死 → SSO Convert → 成功写新 OAuth（旧 RT 废）
SSO 死 → 废弃
```

- 日常续命 **只** RT（~6h）。  
- Convert **只** 作复活 / 补水位原料，不作定时续命。  
- 请求默认：快 RT 可同步；Convert **异步**；RT 死先 **换号**。

---

## 3. CLI 双轴模型（选号与预热共用）

### 3.1 轴 A — 可服务性 `cli_eligibility`（能不能用）

对齐 Python 热表语义（实现名可变）。**封号与 BOT 必须分开**——二者不是同一轴上的同一点。

| 态 | 含义 | 可选？ |
|----|------|--------|
| **READY** | Access 在安全窗内（如 exp > now+60s） | 是（最高服务 rank） |
| **REFRESHABLE** | 有 RT，Access 不可用/将过期 | 是（可先 RT） |
| **ACQUIRE_ALLOWED** | 无可用 Access/RT，允许 SSO Convert/acquire | 是（贵，预热慎用） |
| **TEMP_BLOCKED** | consent/CF / CLI next_eligible 等短冷却 | 否 |
| **DENIED** | 无 scope/能力/auth_failure / 账号禁用·reauth 等 | 否 |
| **BOT_FLAGGED** | **软 bot**（JWT `bot_flag` 非 NP、convert 软标等）；**不是封号** | **是**（rank 最低；只在硬分层落到 L5 时出镜） |
| **CHAT_BANNED**（origin 名；事实字段 `maybe_dead`） | **对话封号**：chat/responses 已确认不可用，JWT 探针仍活 | **否**（硬否决，不进候选、不计 available、不计 warm） |

**与 Python 对照（`~/grok2api`）：**

| 点 | Python | origin 拍板 |
|----|--------|-------------|
| soft bot → `BOT_FLAGGED` | eligibility rank>0，可进 working，层 5 | **对齐**：可进候选，硬分层最后碰 |
| `cli_maybe_dead` | 同步成 `BOT_FLAGGED`（与 bot 混桶）；capacity 里算 unavailable | **收紧（产品）**：独立 **CHAT_BANNED / 硬否决**，**不进池**（比 Python 严，避免已确认对话废号再浪费 attempt） |
| proven + bot | `_cli_layer`：**有成功先 L1/L2**，bot 不压 proven | **对齐** |
| proven + maybe_dead | Python 仍可能按 proven 分层 | **origin：封号优先**，直接出队，不靠「降 L5」 |

**与 Web 配额无关**：CLI 不靠不可靠 remaining 硬杀号；靠 eligibility + 反馈。

### 3.2 轴 B — 优先级层 `cli_layer`（先用谁）

对齐 Python `_cli_layer`（1=最好）。**层只回答「先用谁」；封号不占层竞赛。**

| 层 | 条件 | 含义 |
|----|------|------|
| **1** | **非免费** 且 **CLI 成功过** | 付费/Super/Heavy（或 origin：`BuildSuper`/付费 billing）且 `last_success` 有值 |
| **2** | **CLI 成功过**（含免费） | Try-and-Learn：实战证明可用 |
| **3** | `trusted_source` 且未成功 | 信任进货、未实战 |
| **4** | 其它未证 | 公开 basic 海货默认层 |
| **5** | **soft BOT_FLAGGED 且未成功** | bot 嫌疑未证；**仍可能有用**，最后才碰 |

规则：

- **非免费但未成功 ≠ 层 1**（与 Python 代码一致）。  
- **成功证明覆盖 soft bot**：有 `last_success` → L1/L2，**不**因 soft bot 掉到 L5（对齐 `_cli_layer`）。  
- **`maybe_dead` 不参与 1–5**：在进层之前就被轴 A 否决（CHAT_BANNED）。  
- **选号：硬分层** — 有层 1 候选则不看层 2…5；层内再打分；L5 仅当 1–4 皆空。  
- **预热：同一层序** — 先保证高层 ready；**L5 默认不养**（`warm_target_l5=0`，见 §4）。

### 3.3 成功证明与封号（调度真相，不是装饰）

| 事件 | 更新 | 效果 |
|------|------|------|
| CLI 上游成功 | `last_success_at` / success_count++；清 consecutive_403、**maybe_dead**、soft 403 码 | 升入层 1 或 2；封号可被成功洗白（与 Python `record_cli_success` 一致） |
| 对话 403 + JWT 存活 | consecutive_403；阈值到 → **`maybe_dead`（封号）** | **硬否决出池**；不 RT、不 reauth Web |
| soft bot 标记（JWT bot 等） | 运行时 / 标签；eligibility **BOT_FLAGGED** | 未证 → **L5 仍可选**；已 proven → 仍 L1/L2 |
| 402/429 | next_eligible 冷却 | TEMP_BLOCKED，不废 SSO |
| 401 / JWT 死 | → REFRESHABLE / RT 路径 | **不**标 maybe_dead |

**没有成功证明的号，再「看起来有 token」也不得压过 proven 号。**  
**已封号的号，不得再占用 attempt / warm 库存**（origin 相对 Python 的显式收紧）。

### 3.4 层内评分（选号）

在选定层内（示意，权重可配）：

```text
score =
  eligibility_rank          # READY > REFRESHABLE > ACQUIRE > BOT
  + health
  - cli_inflight
  - fail_count
  - call_count              # 已成功号打散负载
  - consecutive_403
  - remaining_cli_cooldown
  - recent_use_penalty
  ± jitter（可选，防顺序撞号）
```

### 3.5 非免费如何映射到 origin

Python：`pool ∈ {super, heavy}`。  
origin 无同名 pool 列时，**层 1 的「非免费」** 取（满足其一即可，实现时固定一种真值源）：

- `BuildSuperEntitled` / 付费 billing 快照，或  
- 账号 tags / 导入标记 `tier=super|heavy`，或  
- 显式 `cli_tier` 字段  

**禁止**用「曾经 convert 成功」冒充非免费。


### 3.6 分层在哪里做（落点拍板）

**原则：层是「纯函数推导」，不是库里的永久枚举列；选号与预热共用同一函数，禁止两套启发式。**

```text
持久化事实（DB）
  provider_accounts + account_credentials + billing
  + build_cli_profiles（成功证明 / 信任 / CLI 冷却 / generation）
        │
        ▼  ListRoutingCandidates(Build)  只装载事实，不算层
  RoutingCandidate{Credential, Billing, CLIProfile?, …}
        │
        ▼  domain/account.ClassifyCLI(candidate, now)   ← 唯一层真相
  { Layer:1..5, Eligibility, WarmBucket, Selectable }
        │
        ├──────────────┬──────────────────────────────┐
        ▼              ▼                              ▼
  Selector.Acquire   Warm worker tick              指标/管理只读
  （请求选号）        （补水位）                    （观测）
```

| 位置 | 做什么 | 不做什么 |
|------|--------|----------|
| **`domain/account/cli_layer.go`** | `ClassifyCLI` / `CLILayer` / `CLIEligibility` / `WarmBucket` **纯函数**；单测矩阵 | 不 IO、不写库、不读全局配置以外的隐式状态 |
| **`ListRoutingCandidates`** | Build 批量装 `build_cli_profiles`（无行 → 空 profile = 未证默认） | **不在 SQL 里 ORDER BY layer**；层不落 CHECK 枚举 |
| **`gateway/selector.go`（仅 Build）** | ① eligibility 过滤 ② **硬分层**（有 L1 可选则不看 L2…）③ 层内 score（含 `preferFreeBuild` **仅层内**） | 不在 Web/Console 路径调用 ClassifyCLI；不把层写回 Web 健康 |
| **`cli_warm_worker`（仅 Build）** | 同一 `ClassifyCLI` 计 total / fill_order / cap | 不自己发明「谁先养」的第二套优先级 |
| **SQL / 管理列表排序** | 可选展示「推导层」只读；运维筛选可后续加 | 本期不把 layer 当迁移列、不当计费真值 |

**选号流水线（Build Acquire，概念序）：**

```text
1. loadCandidates(Build)          // 含 profile 事实
2. 既有过滤：enabled / auth / cooldown / quota / tags / excluded
3. ClassifyCLI → Eligibility
   - selectReadyOrRefreshableOnly=true（默认）→ 丢掉仅 ACQUIRE / DENIED / TEMP_BLOCKED
4. 硬分层：在仍可选集合里取 min(Layer)；只保留该层
5. 层内 planCandidateIndexes：既有 score + preferFreeBuild(仅层内) + jitter
6. claim 并发槽 → 返回 lease
7. sticky / pinned 路径：仍要过 eligibility；**不**为 sticky 降层去抢未证压 proven
   （若 sticky 号不可用：按现网饱和回落，回落候选仍走 3–5）
```

**预热流水线（同一 ClassifyCLI）：**

```text
1. 扫描 Build 原料（分页/限流，非 20K 无界）
2. ClassifyCLI → WarmBucket + Eligibility
3. total = count(Eligibility==READY 且 bucket 允许进水位)
4. deficit / unproven_cap / effective_floor = min(floor, material)
5. for bucket in fill_order: RT 优先，Convert 有界
```

**关闭开关：** `routing.cli.enabled=false` → Selector **跳过**硬分层，走现网 score；worker 可不启动。便于回滚。

### 3.7 状态如何追踪（写什么、谁写、谁读）

#### A. 持久化（`build_cli_profiles`，Build 1:1）

| 字段 | 谁写 | 谁读 | 用途 |
|------|------|------|------|
| `last_success_at` | Gateway 上游 **成功**（Build only） | ClassifyCLI → proven → L1/L2 | **成功证明** |
| `success_count` | 同上 | 诊断 / 可选层内打散 | 升层不依赖次数，有 at 即可 proven |
| `call_count` | 选中或成功时 +1（Build） | 层内 score 降权 | 负载打散 |
| `trusted_source` | 导入/管理标记 | ClassifyCLI → L3 | 进货信任 |
| `maybe_dead` / `consecutive_403` | Build **对话封号**路径（403+JWT 活） | **CHAT_BANNED 硬否决**；清成功时复位 | **≠** soft bot；≠ convert 污染门闩；≠ Web reauth |
| `next_eligible_at` | 402/429 等 CLI 冷却 | 选号过滤；**不是** `provider_accounts.cooldown_until` 的 Web 语义 | CLI 专用冷却 |
| `token_generation` | Convert 成功 +1 | RT 前校验世代 | 公用 SSO 废旧 RT |
| `last_cli_error_code` | 失败路径 | 运维 / UI 封号原因 | 可观测 |
| `last_explore_at` | warm Phase E 旁路探活 | explore 冷却闸 | 见 §4.7 |
| `updated_at` | 任意写 | 缓存失效辅助 | |

#### B. 推导、不落库

| 名 | 输入 | 输出 |
|----|------|------|
| `cli_layer` | Super? + last_success? + trusted? + **soft bot?**（**不含** maybe_dead） | 1..5；**仅 soft 未证 bot → 5** |
| `cli_eligibility` | Access/RT/next_eligible/auth + **maybe_dead 优先** + soft bot | READY… / BOT_FLAGGED / **CHAT_BANNED** / DENIED… |
| `warm_bucket` | layer + Super∧!proven；**maybe_dead 无桶** | l1 / l2 / nonfree_unproven / l3 / l4 / (l5 默认不养) |
| `Selectable` | eligibility ∈ {READY, REFRESHABLE} **且非** CHAT_BANNED | 选号 working 集 |

> 禁止把 `cli_layer` 缓存成「只在成功时更新的列」却在 Super/billing 变更后不重算——**每次选号/预热现算**。

#### C. 运行时（内存，已有或轻量扩展）

| 项 | 说明 |
|----|------|
| 候选缓存 ~1s | profile 写入后 `invalidateCandidates(Build)` |
| `lastSuccessAt` 内存 map | 现网节流写 health；CLI 成功写 profile 可同路径合并节流 |
| 并发 inflight | 现网 `concurrency` 槽；层内 score 继续用 |
| Worker 内存计数 | tick 内 ready_by_bucket；可暴露 metrics |

#### D. 写路径隔离矩阵（无副作用）

| 事件 | 写 Build profile | 写 Build credentials | 写 Web / Console |
|------|------------------|----------------------|-----------------|
| CLI 上游 2xx/业务成功 | last_success / success_count / 清 403 soft | last_used 可选 | **否** |
| CLI 401 → RT 成功 | 可选清 error | UpdateTokens | **否** |
| CLI 401 → RT 真死 | last_error；Wake warm | refresh_permanent **Build** | **否**（不升格 Web reauth） |
| CLI 429/402 | next_eligible_at | 否（除非策略要求） | **否** |
| CLI 对话封号（403+JWT 活） | consecutive_403 / **maybe_dead** | 否 | **否**（不 RT、不 reauth） |
| soft bot 标记 | 仅 eligibility/层推导输入 | 否 | **否** |
| Convert 成功 | token_generation++ | 新 Access/RT | **否** |
| SSO 证实死亡（显式策略） | 废弃 Build | reauth Build | 仅当策略开启才动 Web |
| Web 请求成功/失败 | **否** | **否** | 既有 Web 路径 |

### 3.8 观测与排障（怎么「看见」层）

| 信号 | 含义 |
|------|------|
| `cli_select_layer` | 本次 Acquire 最终落在哪一层（counter/label） |
| `cli_ready_l1`…`l4` / `cli_ready_total` | 水位库存 |
| `cli_warm_fill_bucket` | 本 tick 补的是哪桶、RT 还是 Convert |
| `cli_unproven_ready` vs cap | 是否触顶 |
| decision / debug 日志 | Build 选号一行带 `layer=` `eligibility=` `account_id=`（无 token） |
| 管理端（可选） | 账号详情只读展示 **推导** layer/eligibility；不提供「手改层」除非改事实字段 |

**排障口诀：**

1. 先看 **eligibility**（能不能用）→ 再看 **layer**（该不该先用）。  
2. 水位不够：看 proven_READY 与 unproven_cap，对照 `号池调度.md` §7。  
3. 「为什么选了未证」：查当时 L1/L2 是否全不可选（冷却/饱和/无 READY）。

### 3.9 与现网组件的边界

| 现网 | 关系 |
|------|------|
| `UpdateHealth` / `cooldown_until` | 仍服务通用失败；**CLI 分层成功证明不靠它**，靠 profile.last_success |
| `preferFreeBuild` | **仅层内**；L1 Super 存在时不会被「偏好免费」跨层抢走 |
| `MarkModelAccessDenied` | 单模型块，不直接改 layer；账号仍可 proven |
| `IsBuildSuper` | 层 1 非免费唯一真值源之一（与 billing） |
| Convert / OAuth（C0） | 只负责「凭证怎么像官方拿到」；层与水位不进 oauth 包 |


---

## 4. 水位预热（与选号同一优先级）

### 4.0 拍板：总水位 = 各类「可能能用」加总

```text
主目标：ready 总数 ≥ warm_target_total
        （READY ≈ 现在就能拿去打 CLI 的号）

谁算「可能能用」而进水位：
  - proven（L1/L2）——最该养
  - 非 proven（L3/L4）——也可能能用，必须参与凑水位
  - 非免费未证（有 token 尚未成功）——也可能能用，参与凑
  - L5 bot 未证 —— 默认不进水位

分层不负责「要不要养」，只负责「缺水时先补谁」：
  补水顺序 L1 → L2 → 非免费未证 → L3 → L4
  直到 total ready 回到 target（或原料耗尽）
```

一句话（产品语言）：

> **水位看合计：各类可能可用的号加起来要够一截库存。**  
> **选号仍看分层：先用更可信的。**  
> 非 proven 不是「不养」，而是「后补、且受 cap，避免 20K 全转」。

### 4.1 目标形态

```text
主：  sum(ready over 允许层) >= warm_target_total
辅：  可选 per-layer soft floor（保证每层至少一点，避免全是未证）
禁：  无序从 basic 海货 Convert 到 total 满
```

建议配置（示意）：

```text
# 主水位（硬目标）
warm_target_total: 500          # 所有允许层 READY 加总（20K 示例；可配）

# 补水优先级（不是互斥配额）
warm_fill_order: [L1, L2, nonfree_unproven, L3, L4]

# 软地板（可选）：有原料时尽量每层有一点，防全是未证
# 实现：effective_floor = min(soft_floor_*, available_material_in_bucket)
# 禁止为追 floor 对无关 basic 无界 Convert
warm_soft_floor_l1: 20
warm_soft_floor_l2: 50
warm_soft_floor_l3: 10
warm_soft_floor_l4: 20          # 非 proven 也保持一些
warm_soft_floor_nonfree_unproven: 10

# 未证上限（防灌爆）：未证 ready 不可超过 total 的一定比例
warm_max_unproven_share: 0.40   # L3+L4+nonfree_unproven 占 total ready 上限
warm_max_unproven_abs: 120

warm_target_l5: 0               # bot 不进
```

**计数定义：**

| 集合 | 计入 total ready？ |
|------|-------------------|
| L1 READY（Super∧成功） | 是 |
| L2 READY（任意成功） | 是 |
| L3/L4 READY（未证但 Access 可用） | **是**（可能能用） |
| 非免费未证 READY | **是**（可能能用；选号层仍非 1） |
| L5 / DENIED / 冷却中 | 否 |

**预热算法：**

```text
tick:
  total = count_ready(allowed_layers)
  if total >= warm_target_total:
      # 可选：只维护 soft floor；否则休眠
      return

  deficit = warm_target_total - total

  for bucket in warm_fill_order:   # L1 → L2 → nonfree_unproven → L3 → L4
      if deficit <= 0: break
      if bucket 是未证 and unproven_ready >= unproven_cap: continue
      if soft_floor(bucket) 未满足: 优先补到 floor
      从该 bucket 取原料:
        READY → skip
        REFRESHABLE → RT
        无 RT / RT 死 → Convert（限流；需 link SSO）
      成功则 total++、deficit--
  原料耗尽 → 告警水位不足
```

### 4.2 谁有资格进「可能能用」水位

| 桶 | 进水位 | 补水优先级 | 说明 |
|----|--------|------------|------|
| L1 非免费∧成功 | 是 | 最高 | 最可信 |
| L2 任意成功 | 是 | 高 | 免费 proven 主力之一 |
| 非免费未证（有/可拿 token） | 是 | 中高 | 可能能用；成功后升 L1 |
| L3 信任未证 | 是 | 中 | 可能能用 |
| L4 其它未证 | 是 | 较低 | **会养一些等着**；受 unproven cap |
| L5 bot 未证 | 否 | — | 默认排除 |

与 Python 差异：

- Python：basic **不**隐式全量 fill。  
- 本设计：**合计水位**；未证 **有界参与**（RT + 限流 Convert），因「可能能用」也要凑库存。

### 4.3 补货动作优先级（单号）

1. 已 READY 且计入水位 → 不做事  
2. REFRESHABLE → **RT**（便宜，优先）  
3. 需 token / RT 真死 → **Convert 复活**（已有 Build + linked Web；贵，限流）  
4. SSO 死 → 废弃，换下一原料  

### 4.3.1 开荒水源（Web→新建 Build）— 实现必达

> 工程细节见 `修改计划.md` **R1**。此处固化产品语义：

- **禁止**「只扫已有 Build、Web 大海永不 Convert」导致 `ready=0 actions=0` 而 target>0。  
- 当 `ready < warm_target_total` 且 Build 侧 material（可 RT/可复活）不足以补缺口时，水位 worker **必须**从 **未 link Build 的 Web SSO** 做有界开荒 Convert。  
- 顺序：仍按 fill_order / 分层偏好；优先 `cli_trusted`（L3）→ 普通未证（L4）；受 `UnprovenCap` + `MaxConvertInflight` + `MaxConvertPerMinute` + `maxPioneerPerTick` 约束。  
- 导入/启动仍 **禁止** 同步全池 20K Convert；开荒是 **缺口驱动旁路**，不是进货同步。  
- 开关：`routing.cli.autoPioneerFromWeb`（默认 true）；关则仅养已有 Build（运维回滚）。

### 4.4 消耗与回补

- 任意计入水位的号被用掉 / Access 过期 → total 降 → 按 fill_order 补到 `warm_target_total`。  
- 未证成功 → 桶从 L4 挪到 L2（或 L1），仍计 total，选号优先级上升。  
- 未证失败 / SSO 死 → 换原料继续凑 total，不连坐 Web。  
- **选号**仍硬分层：有 L1/L2 可选时先用它们；水位里的未证号是 **后备库存**，不是抢 proven 的出镜权。

### 4.5 启动 / 导入

| 阶段 | 行为 |
|------|------|
| 导入 | 只入库；可 Wake；**不**同步转 20K basic |
| 启动 | 同一水位 worker；**按层**补到 target，不是全池 due 狂刷 |
| 运行 | 事件 + timer；始终层序优先 |

### 4.6 写库

- 只写 Build / CLI 计数列；批量、低放大。  
- 选号缓存仅 invalidate Build。  
- 成功证明字段更新必须进热路径可读存储（DB 或 runtime 表），否则分层失效。

### 4.7 水位旁路未证验真（Explore，方案 B）

**目标：** 在用户请求路径之外，有界探测 warm 内 **READY 未证** Build 号，尽早分成 proven / 封号 / 额度冷却 / 传输失败，避免 unproven 长期占水位却从不被对话路径触达。

**原则：**

| 项 | 拍板 |
|----|------|
| 路径 | **仅** `cli_warm_worker` Phase E；不碰 Web/Console 写路径 |
| 默认 | **关闭**（`exploreEnabled: false`）；代理/出口未稳时勿开，否则噪声=封号误标风险 |
| 选号 | **不改**硬分层与用户重试；**封号**（`maybe_dead`）硬否决；**soft bot L5 仍可**在 1–4 空时出镜 |
| 候选 | READY ∧ unproven 桶（L3/L4/nonfree_unproven）∧ **非封号** ∧ 非冷却 ∧ 过 explore 冷却（**不含 L5 养号**） |
| 闸门 | `proven_ready < exploreMinProvenReady` **或** `unproven_ready/ready_total ≥ exploreUnprovenShareTrigger`（任一侧为 0 则忽略该侧） |
| 预算 | 每 tick ≤ `exploreMaxPerTick`；滑动 1 分钟 ≤ `exploreMaxPerMinute` |
| 动作 | 最小 `/v1/responses`（默认 `grok-3-mini`，`max_output_tokens: 8`） |
| 成功 | `RecordBuildCLISuccess` → proven，与网关一致 |
| 对话 403 + JWT 探针存活 | `maybe_dead` / 封号；**不** RT、**不** reauth |
| 401 或 JWT 死 | 仅记日志/可走既有 refresh 路径；**不**标 maybe_dead |
| 429/402 | CLI cooldown，非封号 |
| 传输/超时 | transport outcome，**非**封号 |
| 写库 | `last_explore_at` 冷却闸；其余复用 success / 403 / cooldown 写路径 |

**配置（`routing.cli`，与代码默认一致）：**

```yaml
exploreEnabled: false           # 出口健康后再开
exploreMinProvenReady: 30       # proven READY 薄于此则开闸；0=忽略
exploreUnprovenShareTrigger: 0.35  # 未证占比 ≥ 此则开闸；0=忽略
exploreMaxPerTick: 2
exploreMaxPerMinute: 10
exploreCooldown: 30m            # 同号两次 explore 间隔
exploreTimeout: 25s
exploreModel: ""                # 空 = grok-3-mini
```

**观测日志：** `cli_explore_skipped` / `cli_explore_result` / `cli_explore_tick` / `cli_explore_banned` / `cli_explore_jwt_dead`。

**明确不做：** 用户路径按「一半重试给 unproven」；explore 占 Convert 槽；把超时当封号；默认开启 explore。

---

## 5. 请求路径（调用选号）

```text
CLI 请求:
  Acquire Build
    过滤 eligibility 可选 + next_eligible + cli_inflight
    _cli_layer 硬分层 1→5
    层内 score（§3.4）
  Ensure: READY 直用；REFRESHABLE → RT；勿默认同步 Convert
  上游
  成功 → 记 last_success（可能升层）
  RT 死 → 换号 + 通知预热（高层号优先复活）
  401 → 强制 RT 一次；仍失败换号

Web / Console:
  不触达 CLI 分层与预热写路径
```

---

## 6. 公开 SSO / 公用

- RT 突然 invalid_grant → 预期；高层 proven 号优先进 Convert 队列。  
- 同 SSO 锁；Convert 成功世代 +1。  
- 不因公开噪声对 basic 未证做全池预热。

---

## 7. 量级与组成关系（20K 池 × 水位 500）

### 7.1 全池 OAuth 不可取

| 场景 | 含义 |
|------|------|
| 全池理论 RT（Access=6h） | ~0.93/s（若 20K 全维持 CLI）— **不做** |
| 总水位数百（proven + 有界未证） | 日常 OAuth 远小于 20K 全池 |
| Convert | 为凑 total 有界；inflight/分钟帽 |
| 选号 | 有 L1/L2 时先用它们；水位里的未证是后备 |

### 7.2 假设货源构成（用户量级示意，属性可重叠处见注）

| 标签 | 数量 | 进水位？ | 层/桶 |
|------|------|----------|-------|
| 非免费 | 2 | 是（若可 READY） | **L1**（若已 proven）或 **nonfree_unproven**（若未成功） |
| Proven（有成功证明） | 400 | 是 | **L1**（非免费∧成功）或 **L2**（免费成功） |
| Trusted（信任源、通常未证） | 2500 | 是（有界） | **L3** |
| 无效（SSO 死 / DENIED 等） | 2000 | **否** | 废弃原料，不进 fill |
| 其余无标志或 bot | ≈15098 | 无标志 → **L4** 有界；bot → **L5 默认不进** | |

> **注：** 非免费 ⊆ 池；若 2 个非免费都已成功，则 L1 原料=2，L2 原料≈398。Trusted 与 proven 在实现上互斥于「未证 vs 已证」——已成功的信任号计入 proven 层，不再占 L3。

### 7.3 配置夹逼（水位 500 + 默认 cap）

```text
warm_target_total       = 500
warm_max_unproven_share = 0.40  → 0.4×500 = 200
warm_max_unproven_abs   = 120
⇒ unproven_cap = min(200, 120) = 120

soft_floor_* 实际目标 = min(配置 floor, 该桶可用原料数)
  例：L1 floor=20 但非免费只有 2 → 实际只追 2，不空转 Convert
```

**可达上界：**

```text
max_ready ≈ proven_READY + unproven_cap
          ≤ 400 + 120 = 520   （若 400 proven 全部可 READY）
```

故 **target=500 在「几乎全部 proven 都可 READY」时可达**；  
若 proven 大量日限/冷却导致仅 200 可 READY，则：

```text
max_ready ≈ 200 + 120 = 320 < 500  → 水位告警，不能靠海货 Convert 硬凑满
```

### 7.4 稳态组成（理想：400 proven 均可 READY，2 非免费均已 proven）

按 fill_order：`L1 → L2 → nonfree_unproven → L3 → L4`

| 桶 | 原料 | 进 warm READY | 说明 |
|----|------|---------------|------|
| L1 | 2 | **2** | soft floor 20 被原料夹成 2 |
| L2 | 398 | **398** | proven 尽量养满 |
| nonfree_unproven | 0 | **0** | 已并入 L1 |
| L3 Trusted | 2500 | **100** | 补 total 至 500；100 ≤ unproven_cap 120 |
| L4 无标志 | 大量 | **0** | total 已满，不必再补 |
| L5 bot | 部分 rest | **0** | 默认不进 |
| **合计** | | **500** | unproven 占比 100/500=20% < 40% |

**组成关系一句话：**

> 水位 500 ≈ **几乎整仓 proven（~400）+ 一小截 trusted 未证（~100）**；  
> 2 个非免费再强也只占 L1 的 2 席；  
> 1.5 万无标志/bot **不进** 稳态水位（L4 仅在 proven 不够且 cap 未满时补洞）。

### 7.5 选号时实际出镜顺序（与 warm 库存不同）

即使 warm 里有 100 个 L3 READY：

1. 有 L1 可选 → 只出 L1（最多 2）  
2. L1 耗尽/冷却 → 出 L2 proven  
3. L1+L2 都不可用 → 才出 L3/L4 后备  

因此：**库存可以「proven + 未证」混装；出镜仍 proven 优先。**

### 7.6 soft floor 与「非 proven 也保持一些」

| 意图 | 在本假设下的落点 |
|------|------------------|
| 非 proven 保持一些 | 稳态 L3≈100（由 total 缺口驱动），或 soft_floor_l3/l4 在 total 已满时做可选维护 |
| 不把 20K 拖进 warm | unproven_cap=120 + L5 不进 + fill_order 先 proven |
| L1 floor 看似 20 | **原料夹逼**后只有 2，禁止为追 floor 去 Convert 无关 basic |

### 7.7 若 proven 缩水（更贴近免费日限）

| proven_READY | unproven_cap | 可达 total | 相对 target 500 |
|--------------|--------------|------------|-----------------|
| 400 | 120 | 520 | 达标，L3 补 ~100 |
| 300 | 120 | 420 | 不足，告警；可调高 abs cap 或降 target |
| 200 | 120 | 320 | 明显不足；应靠换号成功扩 proven，而非全池转 |
| 0 | 120 | 120 | 冷启动上限=cap；先探活再养 proven |

冷启动策略：**先有界养未证探测（≤cap）→ 成功升 proven → proven 再占满水位主体。**

---

## 8. 配置建议

```yaml
pool:
  cli:
    # 主水位：各类「可能能用」READY 加总
    warm_target_total: 500          # 20K 池示例；小池可降到 100~300
    warm_fill_order: [l1, l2, nonfree_unproven, l3, l4]
    # 软地板（有原料时尽量每层一点）
    warm_soft_floor_l1: 20
    warm_soft_floor_l2: 50
    warm_soft_floor_l3: 10
    warm_soft_floor_l4: 20
    warm_soft_floor_nonfree_unproven: 10
    # 未证参与水位，但有上限
    warm_max_unproven_share: 0.40
    warm_max_unproven_abs: 120
    auto_fill_non_free: true
    auto_fill_unproven: true          # 非 proven 也参与凑水位（有界）
    auto_fill_require_linked_web: true
    max_refresh_inflight: 25
    max_convert_inflight: 3
    max_convert_per_minute: 20
    access_refresh_advance: 3m
    convert_on_request: false
    # 旁路未证验真（默认关；见 §4.7）
    explore_enabled: false
    explore_min_proven_ready: 30
    explore_unproven_share_trigger: 0.35
    explore_max_per_tick: 2
    explore_max_per_minute: 10
    explore_cooldown: 30m
    explore_timeout: 25s
```

---

## 9. 观测

- `cli_ready_l1` … `cli_ready_l5` / 各层 target  
- `cli_select_layer`（选中落在哪层）  
- `cli_warm_fill_layer`（预热补的是哪层）  
- `cli_proven_total` / `cli_refresh_rt_dead` / `cli_convert_*`  
- `cli_explore_*`（旁路验真：开闸/结果/封号/JWT 死）  
- 断言：Web 路径无 Build refresh/convert；预热未证占比 ≤ cap；`maybe_dead` 不进候选  

---

## 10. 不做清单

- 用 Convert 做 6h 续命。  
- **无 fill_order** 从海货乱 Convert 凑满 total（允许未证参与，但必须 proven 优先 + unproven cap）。  
- 对 basic 未证 **无 cap 的全池** fill/Convert。  
- 非免费未成功却固定压过「免费已成功」。  
- CLI 失败连坐 Web/Console。  
- 请求默认同步 10–15s Convert。  
- 无 SSO 锁并发 Convert。  
- 默认开启 explore，或在出口不稳时把 transport 当封号。  
- 用户路径强制「一半重试给 unproven」（验真走 §4.7 旁路）。

---

## 11. 与 Python / origin 实现映射

| 概念 | Python | origin 目标 |
|------|--------|-------------|
| 层 | `_cli_layer` + `_best_cli` | Build 选号同一函数语义 |
| eligibility | `cli_eligibility_by_idx` + ext 推导 | Build 侧状态列或等价 |
| 成功 | `cli_last_success_at` / success_count | 必落库/热缓存 |
| 预热范围 | super/heavy fill；basic 不隐式全量 | **总水位**；proven 优先补，未证有界参与 |
| 水位 | 无统一 warm_target 环 | **warm_target_total** + fill_order + unproven cap |
| 存储 | SSO+ext | provider_accounts + credentials + link |

---

## 12. 文档关系

| 文档 | 关系 |
|------|------|
| **本文** | CLI/号池调度与预热 SSOT（分层 + 水位） |
| `修改计划.md` | OIDC/C0 与历史池任务 |
| `抓包分析.md` | 协议头与 JWT |
| `~/grok2api/架构设计.md` §6.6 | Python 领域矩阵；冲突时 CLI 分层以本文 + 代码为准吸收 |

---



## 14. 实现落点（origin 代码对照）

| 策略 | 代码 |
|------|------|
| ClassifyCLI 唯一层真相 | `backend/internal/domain/account/cli_layer.go` |
| 成功证明 / CLI 冷却 | 表 `build_cli_profiles` + `AccountRepository` CRUD |
| 选号硬分层 | `gateway/selector.go` filter + `applyCLIHardLayer` |
| 层内 preferFree | `selector_plan.go`（层相同后才比 preferFree） |
| 总水位 worker | `account/cli_warm.go` `RunCLIWarm`：PhaseA soft floor + PhaseB deficit fill_order |
| RT 死 → Convert | `EnqueueBuildCLIConvert` / `reviveBuildCLIViaLinkedWeb`（并行 inflight + 每分钟帽） |
| due 刷新收敛 | `credential_scheduler.refreshDueCredentials` + `shouldAutoRefreshBuildDue` |
| 配置 | `routing.cli`（`infra/config` + `config.example.yaml`） |
| 观测 | lease `cli_layer` 日志；`GetCLIWarmSnapshot`；账号详情 `cliLayer` 等 |
| pinned 选号 | `AcquirePinned` 仍过 CLI eligibility；**不**为 sticky 降层 |
| 回滚 | `routing.cli.enabled=false` |

**验收时以本文策略为准；若代码与本文冲突，先改代码或同步本文。**

## 13. 一句话

**选号：`ClassifyCLI` 硬分层（可信者优先）。预热：同一函数计总水位，缺水按 fill_order 补，非 proven 有界参与。事实在 `build_cli_profiles`，层与 eligibility 现算不落枚举列。RT 养 Access，RT 死再 Convert；只写 Build，不伤 Web。**
