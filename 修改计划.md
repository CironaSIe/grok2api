# 修改计划（dev · 号池运维 / DB 流量 / 水位开荒 / 任务）

> **读者：** 任意编码模型 / 人类执行者。  
> **质量标尺：** **完整、通用、明确、无副作用**。  
> **产品本质：** 号池（进货 → 出站 → 选号 → 反馈）；OpenAI/Anthropic 是外壳。  
> **策略 SSOT：** 同仓 `号池调度.md`（CLI 分层/水位/§4.3.1 开荒；算法方向不改）。  
> **工程 SSOT：** 本文（**2026-07-23 再审：备选设计 + 分波执行 + 影响矩阵**）。  
> **对标：** `~/grok2api` 的管理端协议与写路径精神，**不是**存储 1:1。  
> **基线：** Q1–Q9、CLI P1–P10、C0 OIDC **勿回滚**。  
> **约束：** 未要求则不重启、不 commit；端口 **8888**。

---

## 实现进度

| 任务 | 状态 | 说明 |
|------|------|------|
| Q1–Q9 / CLI / C0 | **完成** | 附录 A |
| R0 问题 + 设计再审 | **完成（本文）** | 备选矩阵、分波顺序、影响范围 |
| **W0-R3b** 批 reconcile | **完成** | `ReconcileProviderLinksMany`；import chunk 批调；失败回退 per-id |
| **W0-R4** Trusted 移出 Convert | **完成** | Convert 对话框去勾选；继承 cli_trusted；导入/列表 tag 保留 |
| **W1-R3-0** snapshot/revision | **完成** | A2：`/accounts/snapshot`+`/changes`；FE 去 page 网络键；mutation bump revision |
| **W2-R3a/d/c/h** 写队列/热路径/sync/投影 | **部分完成** | R3d：CLI 成功合并写+MarkSuccess 不再每请求清 recovery；R3c：默认 sync/import 并发 25→8。R3a 全量写队列 / R3h 同事务投影仍待 |
| **W3-R1** 水位开荒 | 待做 | 至少 R3b 完成；建议 R3a 后全量 |
| **W4-R2/R5/R7** 任务 + 筛 + 前端 | 待做 | R2 先于「可查批 NSFW」 |
| **W5-R6** 请求前 NSFW | 待做 | P2 |
| **R8** 双池逻辑库存 | 中期 RFC | 本期不表合并 |

---

## 0. 用户判定 → 任务（锁死）

| # | 判定 | 任务 |
|---|------|------|
| 1 | 水位不开荒 = 错误 | **R1** + `号池调度.md` §4.3.1 |
| 2 | 进度无异步查询 | **R2** + R7 任务条 |
| 3 | NSFW 全量不合理 | **R5** `pending_nsfw` 默认 |
| 4 | 过滤器无标签 | **R5** + R3-0 本地芯片 |
| 5 | Trusted ≠ Convert 属性 | **R4** |
| 6 | DB 流量海量 | **R3b → R3a/d/c + R3-0** |
| 7 | Web/Console 同 SSO 双池存疑 | **§2 + R3h 止血 + R8** |
| 8 | 翻页不该打库（Python 先例） | **R3-0** |

---

## 1. 问题分层（处理策略）

| 类 | 现象 | 策略 |
|----|------|------|
| **A 无意义读** | `page∈queryKey` → 每页 5～7 SQL | 管理端 **snapshot + 本地分页**（或弱化备选） |
| **B 设计冗余写** | 同 SSO Web+Console 双完整行；密文双写；×14k Reconcile 事务 | 批 reconcile + 投影合写；中期逻辑 identity |
| **C 应合批** | 每请求 UpdateHealth；sync 25 writer；warm 全表扫 | 写队列、上下游分离、coalesce |
| **D 必要写** | 新密文、鉴权失效、RT 成功、禁用/删除、Convert 出 OAuth | **不可吞**；可排队但鉴权类 **立即 flush** |

---

## 2. Web / Console 双池（定性 + 改法）

### 2.1 事实

- 上游：**同一 SSO**；Console 投影复制密文 + 换 adapter。  
- 配额：产品线不完全统一 → **出站面分离合理**。  
- Python：**一行 token** + `quota_console` 等列。  
- origin：`grok_web` / `grok_console` 各一套 `provider_accounts` + `account_credentials` + health/quota。

### 2.2 结论

| 层 | 判断 |
|----|------|
| 库存/进货 | 双完整行 = **低效**（写、reconcile、sync、列表体感） |
| 路由/adapter | 双 Provider **可保留** |
| 本期 | **不**做单表大 migration（R8） |
| 本期止血 | R3b + **R3h**（同事务投影、密文共路、identity 不双打上游） |

### 2.3 双池改造备选（R8 / 远期）

| 方案 | 做法 | 优点 | 缺点 | 选用 |
|------|------|------|------|------|
| **D0 现状+止血** | 两行保留；合写/共密文更新 | 风险最低 | 行数仍 2× | **本期** |
| **D1 主行+投影行** | Web 主库存；Console 仅 channel 表挂同一 credential_id | 密文一份 | 要动 credential FK | R8 候选 |
| **D2 单 identity 多 channel** | `identities` + `channels{web,console,build}` | 最贴号池 | 域模型大改 | R8 首选方向 |
| **D3 纯内存统一** | 像 Python Directory，DB 仅 SSO | 热路径极快 | origin 架构重写 | **不做** |

---

## 3. 读路径：R3-0 Snapshot（备选设计）

### 3.1 业界/先例对照

| 模式 | 要点 | 来源感 |
|------|------|--------|
| **全量 snapshot + revision 增量** | 首屏一次；之后 `since` 变更集；UI 本地 filter/page | Python grok2api 已验证；类似轻量 change-feed |
| **ETag / If-None-Match 分页** | 仍服务端分页，未改页缓存 304 | 翻页仍可能 miss；滤芯片计数难 |
| **虚拟列表 + cursor** | 服务端游标，客户端不持有全量 | 大表友好；**全池 chip 计数/多条件筛选**差 |
| **后台 hydrate** | 先轻量首包，后台灌满本地 store | 折中首屏与完整本地能力 |

> SQLite 侧：高并发下「多连接抢写」易 BUSY；管理读应避免与写风暴叠加（单写者 + 少读放大）。

### 3.2 方案矩阵（R3-0）

| ID | 方案 | 翻页 DB | 芯片/全池筛 | 20k 内存 | 实现量 | 决策 |
|----|------|---------|-------------|----------|--------|------|
| **A1** | 全库 light snapshot（Python 同构） | 0 | 优 | 14k×~0.5–2KB ≈ 7–28MB 需测 | 中 | 单 Provider 全量时可用 |
| **A2** | **按 Provider 分片 snapshot**（默认） | 0（Tab 内） | Tab 内优 | 约 1/2～1/3 峰值 | 中 | **默认采用** |
| **A3** | 分片 + **分块 hydrate**（500/批灌本地） | hydrate 期有序读 | hydrate 后优 | 可控 | 中+ | 内存/超时 fallback |
| **A4** | 保留 page API，但 **单 SQL join** + 客户端页缓存 Map | 仅未缓存页 | 弱 | 小 | 小 | 过渡/降级，不达标则不够 |
| **A5** | 纯 cursor 虚拟表 | 每滚一次 | 差 | 小 | 中 | **否**（运维筛选诉求强） |

**默认路径：A2；** 若单 Provider light 体积与耗时合格可升 **A1**；若超时/OOM → 自动/配置降 **A3**。A4 仅作前端未迁完时的后端优化，**不**算 R3-0 完成。

### 3.3 默认协议（A2）

```http
GET /api/admin/accounts/snapshot?provider=grok_web&view=light
→ {
  "revision": 12345,
  "provider": "grok_web",
  "generatedAt": "...",
  "items": [ /* AccountSnapshotItem */ ],
  "count": 14035
}

GET /api/admin/accounts/changes?since=12345&provider=grok_web
→ {
  "revision": 12350,
  "upserted": [ ... ],
  "deletedIds": [1,2],
  "hasMore": false
}
```

| 约定 | 内容 |
|------|------|
| **light 字段** | id, name, email?, status, enabled, authStatus, tags[], quota 摘要, nsfw 标志, trusted, buildLinked, cliLayer/eligibility（Build）, updatedAt；**无**密文、无长 error 栈 |
| **revision** | 账号域全局单调；**每批 flush +1**（不是每行 +1）；与 settings revision **隔离** |
| **分页** | 客户端 `filter → slice`；`page` **移出** 主 query 网络键 |
| **旧 API** | `GET /accounts?page=` **保留**至前端切换完成；文档标 transitional |
| **Summary** | 优先从已加载 snapshot 派生；或 60s TTL `Summarize`；禁止绑每页 List |

### 3.4 修改方式（落地步骤）

1. **Schema**  
   - `app_meta(key, value)` 或沿用已有 meta：`account_revision`。  
   - 可选：`provider_accounts.row_revision BIGINT`（changes 索引）；无则 `account_change_log(id, rev, op, provider)` 限长。  
   - 启动：`AutoMigrate` 幂等；旧库 revision 从 1 起。
2. **仓储**  
   - `RuntimeSnapshot(ctx, provider, view)`：**目标 1～2 SQL**（主账号 + billing 摘要 LEFT JOIN；Build 再批 IN cli_profile）。  
   - `ScanChanges(ctx, since, provider, limit)`：`WHERE row_revision > since` ORDER BY rev LIMIT。  
   - 写路径包装：`withAccountRevision(tx, func)` 批末 bump。
3. **应用层**  
   - `Service.Snapshot` / `Service.Changes`；组装与现 `View` 同源映射，但 **禁止** 再调 5 次 Get*（join 或一次批 ID）。  
   - SumTokens：light **可省略** 或异步字段；勿阻塞 snapshot。
4. **HTTP**  
   - `handler.go` 注册 snapshot/changes；鉴权同 list。  
   - OpenAPI/swagger 注解可选。
5. **前端**  
   - `AccountStore`：`load(provider)` → items Map；`applyChanges`；`useMemo` filter + page slice。  
   - `accounts-page`：`queryKey` **去掉 page**（或仅本地 state）；Tab 切换换 provider 再 load。  
   - 聚焦/30–60s：`changes?since=`。  
   - 失败：toast + 可 fallback 旧 list API（feature flag）。

### 3.5 影响范围（R3-0）

| 层 | 路径/符号 |
|----|-----------|
| Schema | `relational/schema.go`、`models.go`（meta / row_revision） |
| Repo | `account_repository.go`、新 `account_snapshot.go`（可） |
| Port | `repository/account.go` 接口增方法 |
| App | `application/account/service.go` |
| HTTP | `transport/http/account/handler.go`、`server.go` 若需 |
| FE | `accounts-api.ts`、`accounts-page.tsx`（~79KB，改动面大） |
| 测试 | repo snapshot 单测；handler 集成；FE 可手工 |
| 配置 | `admin.accounts.snapshotMode: provider\|full\|chunked`（可选） |
| 运维 | 首次 14k snapshot 延迟；日志 `account_snapshot_ms` / `items` |

### 3.6 验收 / 回滚

| 验收 | 标准 |
|------|------|
| 翻页 | 同 provider 下 page 1→10 **无** List/snapshot 重复全量 |
| 空 changes | 成本接近索引点查 |
| 体积 | light 单 provider p95 &lt; 约定阈值（先 32MB 响应警戒） |
| 安全 | 响应无明文 token |

**回滚：** FE flag 切回 page List；路由可留只读。

---

## 4. 写路径：R3a–h（备选 + 默认）

### 4.1 写放大证据（摘要）

```text
导入 14k Web + auto Console:
  UpsertMany chunk=100     ~140 tx
  Reconcile 每号 1 tx      +14k   ← R3b
  Console 再 Upsert+Reconcile
  accountsync ×25 每号多写        ← R3c
热路径: MarkSuccess → UpdateHealth 每请求 ← R3d
SQLite: busy_timeout 5s, MaxOpenConns=16 vs 多 writer → BUSY
```

### 4.2 写模型备选

| ID | 方案 | 说明 | 选用 |
|----|------|------|------|
| **W1 应用写队列** | channel 合并同 id 字段；**单 flusher** 批事务；鉴权/删除同步旁路 | 可控、可关 | **默认** |
| **W2 连接池=1** | SQLite `SetMaxOpenConns(1)` 全局串行 | 简单但读也被堵 | 仅辅助，不单靠 |
| **W3 内存 Directory** | 调度态全内存 + 批 persist | 最贴 Python | **本期不做** |
| **W4 仅批 reconcile** | 不动热路径 | 导入立刻好 | **W0 先做** |

> 业界共识：SQLite 适合 **单写多读（WAL）**；应用层合并写比「多 goroutine 各开事务」稳。

### 4.3 子任务定义

| ID | 修改方式 | 关键符号 | 优先级 |
|----|----------|----------|--------|
| **R3b** | `ReconcileProviderLinksMany(ids []uint64)`；chunk 内一 tx；导入/sync 改调 Many | `account_links.go`、`provider_links.go`、`persistImported*` | **P0 最先** |
| **R3a** | `writequeue`：HealthPatch / CLISuccess / IdentityMeta / Capabilities；`workers=1`（SQLite） | 新包、`selector.go`、`accountsync`、`database.go` | P0 |
| **R3d** | 成功态内存 coalesce，`healthFlushSec`；**失败/reauth 立即写** | `gateway/selector.go` MarkSuccess/Failure | P0 |
| **R3c** | sync：`upstreamWorkers` 只 HTTP；结果入队；Has* 批预取 | `accountsync/service.go` | P0 |
| **R3h** | 导入/SyncWeb→Console：**同事务** Web+Console+link；密文更新共函数；Console **不**重复 identity 上游 | `web_console_sync*.go`、import | P1 |
| **R3e** | 旧 List 1～2 SQL（过渡） | `service.go` List | P2 若 A2 完成可砍 |
| **R3f** | warm 只扫缺口相关 | `cli_warm.go` | P1 随 R1 |
| **R3g** | revision bump 挂写队列 flush | 与 R3-0 共用 | P0 |

### 4.4 配置草图

```yaml
database:
  writeQueue:
    enabled: true
    workers: 1
    batchSize: 200
    flushIntervalMs: 200
  import:
    credentialChunkSize: 100   # 已有 100
    reconcileBatchSize: 100
  sync:
    upstreamWorkers: 8
    writeViaQueue: true
  hotPath:
    healthFlushSec: 5
```

### 4.5 影响范围（写路径）

| 组件 | 文件 |
|------|------|
| 连接 | `relational/database.go`（可选读多写单说明） |
| Links | `account_links.go`、`provider_links.go`、import 路径 |
| 队列 | **新建** `internal/infra/persistence/writequeue/` 或 `application/accountwrite/` |
| 网关 | `gateway/selector.go` |
| 同步 | `accountsync/service.go`、worker 数配置 `infra/config` |
| 投影 | `application/account` web console sync / import |
| 测试 | links many、queue coalesce、sync 不直写 |
| 指标 | `db_write_queue_depth`、`reconcile_tx_count`、`sqlite_busy` |

### 4.6 验收

| 场景 | 标准 |
|------|------|
| 导入 1k+auto Console | reconcile **tx ≈ ceil(n/batch)**；BUSY≈0 |
| 持续成功请求 | UpdateHealth QPS 下降一个数量级（抽样） |
| `writeQueue.enabled=false` | 行为正确直写 |

---

## 5. R1 水位开荒

| 项 | 内容 |
|----|------|
| 规格 | `号池调度.md` §4.3.1 |
| 修改 | `cli_warm.go`：ready&lt;target 且 material 不足 → 有界选未 link Web → Convert |
| 配置 | `autoPioneerFromWeb`、`maxPioneerPerTick`、`pioneerPreferTrusted`（settings.cli 可暴露） |
| 依赖 | **至少 R3b**；推荐 **R3a** 后提高 `maxPioneerPerTick` |
| 影响 | `cli_warm.go`、account 列表「可开荒」查询、config/settings DTO、日志 `cli_warm_pioneer_*` |
| 无副作用 | 失败不废 Web；受 `MaxConvertPerMinute`；启动不 20k 全转 |

---

## 6. R2 异步任务

### 6.1 备选

| 方案 | 说明 | 选用 |
|------|------|------|
| **T1 进程内 task registry** | id → state；SSE+GET；重启丢失 | **默认**（对齐 Python 常用形态） |
| **T2 DB 持久 task** | 重启可恢复 | 二期 |
| **T3 仅增强现有 SSE** | 无 taskId | **否**（不满足「可查询」） |

### 6.2 协议

```text
POST 现有 batch/convert/scripts → 可同时 { taskId } + 可选仍 SSE
GET  /api/admin/tasks/:id
GET  /api/admin/tasks/:id/stream
GET  /api/admin/tasks/active
POST /api/admin/tasks/:id/cancel
```

### 6.3 影响

| 层 | 路径 |
|----|------|
| 新包 | `application/admintask` 或 `pkg/asynctask` |
| HTTP | `handler.go` 批入口改造；现 `accountEventStream` 可挂 task |
| FE | 全局 TaskDock；`restoreActiveTasks` |
| 覆盖 | import、convert、console sync、scripts/NSFW、quota 批 |

---

## 7. R4 Trusted

| 改 | 文件 |
|----|------|
| Convert body **忽略/删除** `trustedSource` UI | `accounts-page.tsx`、i18n |
| API 仍可兼容字段但文档废弃 | `handler` convert DTO |
| 继承逻辑保留 | convert 成功读 Web tag |

**影响极小，可 W0 与 R3b 并行。**

---

## 8. R5 标签 / NSFW 批量

| 项 | 内容 |
|----|------|
| 列表筛 | tag / nsfw / buildLinked / trusted；**优先 R3-0 本地**；服务端 filter 供批任务扫库 |
| scope | `pending_nsfw`（默认）\| `filter` \| `ids` \| `all_force` |
| 幂等 | 已开启 skip，不打上游 |
| 依赖 | R2 任务条；R3-0 芯片体验 |

影响：`run-scripts` handler、service 扫描、`web-account-scripts.tsx`、repo tag/nsfw 条件。

---

## 9. R6 请求前 NSFW（P2）

租约后 tag/profile 短路 → 脚本 → 写队列；配置默认关或随 allowNSFW。  
影响：`gateway` / web adapter、profile 字段。

---

## 10. R7 前端收口

随 R3-0 / R2 / R4 / R5：AccountStore、TaskDock、Convert 瘦身、芯片、pending NSFW 主按钮。  
主文件：`accounts-page.tsx`、`accounts-api.ts`、scripts/settings 对话框、i18n。

---

## 11. 执行顺序（再审确认 · 分波）

> 原则：**先止血写事务风暴与最小 UX 错语义 → 再管理读协议 → 再热路径队列 → 再开荒 → 再任务化批运维。**  
> 读/写两条可 **部分并行**（不同文件）。

```text
W0  快速止血（可并行）
    ├─ R3b  ReconcileMany（导入 BUSY / 万级 tx）     ★写首选
    └─ R4   Convert 去 Trusted 勾                     ★半日级

W1  管理读主线
    ├─ R3-0 后端 snapshot/changes + revision bump 点
    ├─ R3-0 前端 AccountStore + 去 page 网络键
    └─ （可选）R3e 旧 List 减扇出，仅过渡

W2  写路径结构化
    ├─ R3a  写队列单写者
    ├─ R3d  热路径 health coalesce
    ├─ R3c  accountsync 上下游分离
    └─ R3h  Console 投影合写 / 密文共路

W3  号池补水
    ├─ R3f  warm 扫描收敛（可与 R1 同 PR）
    └─ R1   水位开荒（autoPioneer*）

W4  运维体验
    ├─ R2   异步任务模型（先后端 registry）
    ├─ R5   pending_nsfw + 标签筛（接任务）
    └─ R7   TaskDock / 芯片 / 文案收口

W5  增强
    ├─ R6   请求前 NSFW
    └─ R8   双池逻辑库存 RFC（独立）

验收门：§13
```

### 依赖图（简化）

```text
R3b ─────────────┬──────────► R1
R3a ◄── R3b 可选  ┤
R3d ──►（独立）   ┤
R3-0 ────────────┼──► R5 芯片体验
R2 ──────────────┴──► R5 批任务 / R7
R4 独立
R3h 可与 R3a 并行
```

### 顺序调整说明（相对上一版）

| 原顺序 | 再审 | 原因 |
|--------|------|------|
| R3-0 绝对第一 | **W0 先 R3b（+R4）**，R3-0 紧随 | 导入 `SQLITE_BUSY`/14k tx 是 **写路径 P0**；R3b 改动面小于全站 snapshot 前端 |
| R3a 紧贴 R3b 前 | R3b 先于 R3a | Many-reconcile **无**新队列基建也能上 |
| R1 在 R3 全完成后 | **R3b 后可有界试开荒**；全量 pioneer 仍待 R3a | 避免开荒写再次打爆 |
| R2 较后 | 仍 W4，但 **批 NSFW 强依赖** | 无 taskId 仍不满足用户 #2 |

---

## 12. 全局影响矩阵

| 域 | 触碰 | 不动 |
|----|------|------|
| DB schema | meta revision、可选 row_revision/change_log | 不合并 Provider 表（本期） |
| 密文 | 共路更新；snapshot 不出台明文 | OIDC/C0 协议 |
| 选号热路径 | MarkSuccess 写频率；候选缓存仍可 1s TTL | 分层算法 SSOT |
| 管理 API | 增 snapshot/changes/tasks；旧 list 保留 | 推理 API 契约 |
| 配置 | writeQueue、import.reconcileBatch、cli pioneer、snapshotMode | 端口 8888 |
| 前端 | accounts 页架构级 | 非账号页可不动 |
| 测试 | repo/queue/warm/handler | 勿删 Q1–Q9 相关测 |
| 运维 | 日志字段、可关开关 | 强制用户迁库 |

---

## 13. 无副作用清单

| 做 | 不做 |
|----|------|
| 开关默认安全（队列可关、pioneer 有界、NSFW pending） | 启动全量 20k Convert |
| 鉴权失效 **同步落盘** | 成功 last_used 可延迟 |
| 账号 rev ≠ settings rev | 混用 revision |
| 开荒失败隔离 Web | Convert 失败删 SSO |
| 双池止血不假合并 | 静默改 Provider 语义 |
| light snapshot 无 token | 管理接口泄露密钥 |

---

## 14. 验收总表

| 域 | 标准 |
|----|------|
| 导入 | 1k Web+Console：reconcile tx≈chunk；BUSY≈0 |
| 列表 | 同 Tab 连翻 10 页无全量重复拉取 |
| 水位 | Web 有、Build 空 → pioneer 有日志/动作 |
| 任务 | 批操作有 taskId；刷新可 active 恢复 |
| NSFW | 默认 pending-only |
| Trusted | Convert 无勾 |
| 回归 | CLI 分层选号、OIDC、egress 默认行为不坏 |

---

## 附录 A — 基线（勿回滚）

| 项 | 状态 |
|----|------|
| Q1–Q9 DefaultEgress / 导入 Console / trusted / CLI 筛 / settings.cli | 完成 |
| CLI P1–P10 分层水位 | 完成（缺开荒 = R1） |
| C0 OIDC | 完成 |

## 附录 B — 代码锚点（origin）

| 主题 | 路径 |
|------|------|
| List 多扇出 | `application/account/service.go` `List` |
| page queryKey | `frontend/.../accounts-page.tsx` ~L191 |
| Reconcile 单号 | `relational/account_links.go` `ReconcileProviderLinks` |
| SQLite | `relational/database.go` busy_timeout/WAL/MaxOpenConns |
| sync 并发 | `application/accountsync` |
| MarkSuccess | `application/gateway/selector.go` |
| warm | `application/account/cli_warm.go` |
| 批 SSE | `transport/http/account/handler.go` `accountEventStream` |
| CLI 配置 DTO | `transport/http/settings/handler.go` `cliRouting*` |
| 路由注册 | `handler.go` `/accounts/*` |

## 附录 C — Python 对照

| 主题 | 路径 |
|------|------|
| snapshot/changes | `.../admin/tokens.py` |
| 前端 TokenStore | `.../admin/account.html` |
| revision | `platform/storage/revision.py`、`control/account/backends/sql.py` |
| AsyncTask | `platform/runtime/task.py`、`admin/batch.py` |
| 内存号池 | `dataplane/account/` |
| 单行多配额 | SQL `quota_console` 等 |

## 附录 D — 设计备选速查

| 问题 | 默认 | 备选 | 否选 |
|------|------|------|------|
| 管理列表 | A2 provider snapshot | A1 full / A3 chunked | A5 纯 cursor |
| 写合并 | W1 应用队列 | W2 conn=1 辅助 | W3 全内存重写 |
| 双池 | D0+R3h | D1/D2 = R8 | 假合并骗前端 |
| 任务 | T1 进程内 | T2 DB 持久 | T3 仅 SSE |

---

## 一句话

**W0 先砍导入万级 Reconcile 与 Convert 上的 Trusted 错位；W1 用按 Provider 的 snapshot/revision 消灭翻页打库（全量/分块为备选）；W2 单写队列+热路径合并+sync 拆分+Console 合写；W3 有界开荒；W4 任务化批运维与筛选——双池出站可分、库存双行本期止血、表合并留 R8。**
