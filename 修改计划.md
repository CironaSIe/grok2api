# origin 移植修改计划（dev → origin）

> **读者：** 任意编码模型 / 人类执行者。  
> **属性要求：** **完整、通用、明确、无副作用**。  
> **产品本质：** 号池调度（选号 → 调用 → 即时反馈）；OpenAI/Anthropic 是外壳。  
> **范围：** 将 `~/grok2api`（Python dev）中**值得**的机制迁入本仓 Go origin。  
> **基线：** 分支 `dev`（与 `origin/main` 对齐后切出，v3.0.7+）。  
> **输入证据：** `~/grok2api/移植目标.md`、`对比结果.md` §一、origin 热路径与重试复核。  
> **执行约束：** 一次只做一个任务编号；禁止顺手大重构；禁止整包抄 `xai_oidc.py`；禁止照搬 dev §1.3 错误 UA。

---

## 0. 质量标尺（每次动手前默读）

| 标尺 | 含义 | 检查问句 |
|------|------|----------|
| **完整** | 检测→状态→选号→反馈→清标/恢复→测试/配置/迁移闭环 | 漏了「谁写、谁读、谁清、谁测」吗？ |
| **通用** | 策略按 **错误 class / 操作风险**，不写死「仅免费」分支散落全库 | 付费号走同一套分类是否仍正确？ |
| **明确** | 文件、符号、默认值、验收、非目标写死 | 另一个模型能否不猜直接改？ |
| **无副作用** | 默认配置行为可预期；不误伤 sticky/视频钉号/egress 防重放/传输超时语义 | 会不会双扣、误杀号、刷日志泄密？ |

### 0.1 号池特征 → 设计默认

| # | 特征 | 设计默认 |
|---|------|----------|
| ① | 账号能力分层（含 1010 半残） | 标签/模型封锁，不全局废号 |
| ② | 大批量进池/刷新易 429 | **导入/同步** 批末复检；运行时 429 不堆玄学 |
| ③ | 同模型多号可选 | 换号优先；近分 jitter |
| ④ | 故障转移要快 | 默认零冷却；传输超时不 MarkFailure |
| ⑤ | 运维动作要可区分 | reauth 原因；废号 vs 可重导 |

### 0.2 池子构成（现场）

| 类型 | 现实 | 策略 |
|------|------|------|
| **免费号** | 池内绝大多数 | **成功率优先**：敢换号、少冷却 |
| **付费号** | 少数；**极少 429** | 同一套 class 策略；额外只守「写出不重放 / sticky attempts=1 / 凭证真死」 |

**通用写法：** 代码用 `FailureClass` / 操作类型分支，**不要** `if freeAccount { ... }` 散落；默认参数按免费池成功率标定，付费自动受益且不被限流逻辑绑死。

### 0.3 不变量（禁止破坏）

1. 传输超时 / 非账号作用域失败 → **不** `MarkFailure` 长冷却，可换号。  
2. SSO 401 与 CLI OAuth refresh 分层（origin `AuthType` + `RefreshPermanent`）。  
3. sticky / previous_response 归属 → chat **attempts=1**。  
4. **视频 generate 钉 job 账号**，禁止生成阶段换号。  
5. egress：`WroteRequest` 后 **禁止** 重放 POST。  
6. 媒体 **后处理**（下载/落盘）失败 ≠ 上游生成失败：不换号重生、不（或谨慎）计账号失败。  
7. 日志/审计不落 cookie、refresh_token、完整上游 body。  
8. 多实例：refresh/转换继续用现有分布式锁与 scheduler，不引入进程内全局互斥替代。

### 0.4 重试预算（定稿）

| 路径 | 换号 | 同号恢复 | 后处理重试 |
|------|------|----------|------------|
| chat/responses（非归属） | 是，`maxAttempts` 默认 3 | CLI refresh 一次 | — |
| chat 归属/sticky | **否**（attempts=1） | 按现有 | — |
| 生图 generate | **是**（T1b 补齐） | egress 403 可同号再试一次 | 下载/存盘 3 次 |
| 生视频 generate | **否** | — | 下载/落盘/写库 3 次 |
| 导入同步 | 账号维重试 rate-limit | — | — |
| 代理连接 | — | 未写出可重试 | — |

---

## 1. 总表与执行顺序（SSOT）

### 1.1 任务总表

| 序 | ID | 任务 | 优先 | 主落点 |
|----|----|------|------|--------|
| 1 | **H1** | OAuth/转换按端点请求头 | P0 | `cli/oauth.go`、`web/sso_build.go` |
| 2 | **T2** | 导入同步 429 批末复检 | P0 | `application/accountsync` |
| 3 | **O1** | SSO→Build 批转换韧性 | P1 | `sso_build` + account convert（可并入 H1） |
| 4 | **W1** | 请求前年龄门闩：Pending ensure，失败换号 | P1 | gateway Web + `EnsureWebBirthDateOnce` |
| 5 | **T1+T1b** | no_image + 生图 generate 换号 | P0 | domain tags、`image.go`、selector |
| 6 | **T3** | **仅非流** 动态超时 | P0 | `pkg/chattimeout` + gateway/provider |
| 7 | **T4** | 冷却分型（默认零冷却） | P0 | `selector.MarkFailure`、`failure.go` |
| 8 | **T5** | reauth 原因可运营 | P1 | domain + API + UI |
| 9 | **H2** | CLI 推理头配置化 | P1 | `cli/adapter.applyHeaders` |
| 10 | **T6** | 近分 jitter | P1 | `selector_plan.go` |
| 11 | **T7** | shape + out_bytes | P1 | `pkg/jsonshape`、timing |
| — | T8/O2 | 决策日志 / repair | P2 | 后置 |
| — | T9 | CLI/Console 非流 SSE | — | **不做** |

### 1.2 执行顺序（复制即用）

```text
1  H1     OAuth/转换请求头
2  T2     导入 429 批末复检
3  O1     SSO→Build 韧性（若未并入 1）
4  W1     请求前查库：Pending→ensure 一次；失败换 AdultReady
5  T1+T1b no_image + 生图换号（同波）
6  T3     非流动态超时
7  T4     零冷却分型（单独 PR，可配置回滚）
8  T5     ReauthReason
9  H2     CLI 推理头配置（默认=现状）
10 T6     近分 jitter
11 T7     shape / out_bytes
（可选）T8 → O2
（禁止）视频 generate 换号 / PKCE 整包 / 运行时 429 大改 / 照搬错误 UA
（禁止）每个请求都同步打 set-birth-date（见 W1）
```

### 1.3 不做清单

- 重做 SSO 真死白名单（origin 更优）
- 管理 UI 重写、产品层统一路由、本地 per-SSO 锁替代 Redis
- 视频 generate 换号、双扣风险路径的「激进重放」
- 默认改变 CLI 推理头指纹（H2 仅配置化）
- 流式请求套动态墙钟 total

---

## 2. 移植处理通识（所有任务共用）

### 2.1 落地分层

```text
domain     状态/标签/枚举与不变量
persist    schema 可空默认、向前兼容迁移
application 编排：选号、失败 class、换号、清标
provider   协议解析、请求头、超时注入点
transport  API DTO 暴露（只读展示优先）
frontend   徽章/筛选（可二期）
test       单测锁行为；禁止依赖真实上游
```

### 2.2 无副作用清单（改前对照）

| 风险 | 防护 |
|------|------|
| 双计费/双生成 | 视频钉号；egress 写出不重放；后处理不调 generate |
| 误废号 | 1010→tag 非 reauth；传输失败不 MarkFailure |
| 容量塌缩 | T4 默认零冷却；禁止 Retry-After 绑死整号（额度走窗/模型级） |
| 配置惊喜 | H2/T3 流式/T4 回滚开关：默认=旧行为或显式文档化的新默认 |
| 日志泄密 | 只打 reason/class/status/shape 键树，不打 body |
| 多实例打架 | 转换/refresh 用现有 lock key，不新造进程级全局锁 |

### 2.3 配置与默认值原则

- **新行为若改变容量曲线**（T4）：必须有 `routing.cooldownMode=legacy|class` 或等价开关，**默认 `class`（零冷却）** 前在 commit message 写明。  
- **新行为若改变上游指纹**（H2）：**默认必须等于当前硬编码**。  
- **新列/新字段**：可空或默认空；读路径兼容旧库。

### 2.4 验证通式

```text
go test ./backend/internal/<touched>/...
```

必要时再扩到 `./backend/internal/application/gateway/...`。  
禁止为过测试而削弱不变量。

---

## 3. 任务卡片（处理思路 + 关键细节）

### #1 H1 — OAuth/转换请求头矩阵（P0）

**目标：** 按**端点类**统一身份头，提高 SSO→Build / token refresh 成功率。

**处理思路：**

1. 定义端点类枚举：`OAuthForm`（device/token/refresh）、`OAuthBrowser`（verify/approve/accounts）、`CLIInference`（本任务不改推理）。  
2. `OAuthForm` / `OAuthBrowser`：**浏览器 UA** + 必要 Accept/Content-Type；token 侧按抓包决定是否加 `X-XAI-Token-Auth`（信号在 Token-Auth，不在 CLI UA）。  
3. 复用 `browserheaders.ApplyChromiumClientHints` 于 Browser 类。  
4. **禁止** form 使用 `grok-pager`/`grok-shell` CLI UA；**禁止** login-config 使用 `curl/7.80.0`（若加预检）。

**关键细节：**

| 文件 | 动作 |
|------|------|
| `infra/provider/cli/oauth.go` | `postForm`/`exchange` 注入头；单测 table per URL |
| `infra/provider/web/sso_build.go` | `do()` 头与 Cookie 并存；Hints 与 UA 版本一致 |
| 测试 | 断言 UA 子串、禁止 CLI UA 正则 |

**无副作用：** 不改推理 `applyHeaders`；不改 token 持久化格式；不改 Convert 状态机主流程。

**验收：** 单测锁头；现有 conversion 测试仍过。

---

### #2 T2 — 导入同步 429 批末复检（P0）

**目标：** Phase2 同步遇 rate-limit 不永久半残。

**处理思路：**

1. 在 `accountsync.syncAccount` 识别可重试限流（HTTP 429 / 既有 provider 错误包装）。  
2. **单账号：** 有限次即时重试（短 backoff）。  
3. **批次：** 首轮结束后收集 failed，仅对 rate-limit 子集再跑 1..N 轮（批末复检）。  
4. 401/真死：不进复检集，走既有 reauth。  
5. 配置：`batch.importSyncMaxRounds`（默认 3–5）、`importSyncBackoff` 区间。

**关键细节：**

- 与「运行时 chat 429」隔离：只动 accountsync。  
- 总时间/轮次硬上限，防死循环。  
- 进度回调：复检轮次可观测（日志字段 `sync_round`）。

**无副作用：** 不改变已成功账号；不提高默认同步并发到打爆上游（可保持现有 worker）。

**验收：** mock 2×429→成功；超轮次 Failed；401 一次失败。

---

### #3 O1 — SSO→Build 韧性（P1，可并 H1）

**处理思路：** 转换错误 class 化（SSO 死 / 限流可重试 / 网络可重试）；批转换对可重试 class 退避；错误码稳定给前端。  
**不做：** PKCE、重写 `xai_oidc`、换 TLS 栈。  
**无副作用：** 幂等 convert（已有 missing/all 策略）保持。

---

### #4 W1 — 请求前年龄门闩 + 失败换号（P1）

**运营语义（按你的原话定稿）：**

> 每次请求前看这个 SSO 在库里是否 **还没满 18 / 未完成年龄前置**；  
> 若是 → **执行一次** `set-birth-date`（随机满 18）并 **回写数据库**；  
> 若这次更新失败 → **本请求不要死磕该号**，换号重选 **库内已是 18+** 的账号，  
> 避免下游要 NSFW（或上游按年龄拒）时被未成年档挡掉。

**不是：** 每个已成年号每次请求都打 `set-birth-date`。  
**是：** 仅当库记录为「未满 18 / 无年龄前置」时，在该号被选中后 **ensure 一次**；失败则 SWITCH。

---

#### 状态定义（明确、可测）

库内用既有字段表达「是否满 18 前置」，不另造模糊布尔除非必要：

| 库状态 | 条件 | 含义 |
|--------|------|------|
| **AdultReady（满 18 可用）** | `WebBirthDateSetAt != nil` **或** `WebNSFWEnabledAt != nil` | 已具备年龄前置；请求前 **零** set-birth 调用 |
| **AdultPending（未满 18 / 未备齐）** | 两字段皆 nil | 请求前必须 **ensure 一次** |

> 说明：上游很少返回「真实年龄」；号池只保证「我们已成功写过成年生日 / 已开过 NSFW」。  
> `WebNSFWEnabledAt` 可视为更强标记（开 NSFW 前必过生日）。

年龄写入：继续用现有 **20–40 随机日**（已是满 18）；配置可暴露 `minAge=18`，默认 min=20 更稳。

---

#### 请求路径状态机（完整）

```text
selector.Acquire → 账号 A
EnsureCredential(SSO 有效)
  if provider != Web SSO: 跳过 W1
  if AdultReady(A): 直接上游
  if AdultPending(A):
      ok = EnsureWebBirthDateOnce(A)   // 同号 lock/singleflight；成功或 AlreadySet → 写 WebBirthDateSetAt
      if ok: 上游
      if !ok:
          exclude[A] = true            // 本请求不再选 A
          // 可选：短暂标记「年龄脚本失败」避免连环 ensure，或仅 exclude 本 attempt
          continue attemptLoop         // 重选 AdultReady 优先
```

**选号偏好（通用、减副作用）：**

- Web 路径（或 `AllowNSFW`/媒体路径）`Acquire` 时：  
  - **优先** `AdultReady`  
  - `AdultPending` 仍可被选中（池里全是新号时必须能推进），但选中后走 ensure  
- ensure 失败后的后续 attempt：**硬过滤** `AdultPending`（只选已 AdultReady），避免 N 个新号连续 ensure 打爆 set-birth  
  - 配置：`web.adultPendingMaxEnsuresPerRequest` 默认 **1**（每请求最多对 1 个 pending 号尝试 ensure）

---

#### 与「每请求」的关系（无副作用）

| 库状态 | 每次请求是否打上游 set-birth？ |
|--------|--------------------------------|
| AdultReady | **否** |
| AdultPending，且本请求尚未对该号 ensure 过 | **是，一次** |
| AdultPending，ensure 失败已 exclude | **否**（换号） |
| 同号并发两个请求 | lock：只一次上游，另一次等结果 |

因此：**逻辑上「每次请求前都检查库」**；**物理上只有 Pending 号才会打上游，且每号成功后永远不再打。**

---

#### origin 复用

| 能力 | 位置 |
|------|------|
| 随机 20–40 生日 | `randomWebBirthDate` / `setRandomWebBirthDate` |
| AlreadySet / 锁定 429 → 仍 Mark | `ErrBirthDateAlreadySet` |
| 单号脚本锁 | `acquireWebAccountScriptLock` |
| 批处理补齐 | `RunWebAccountScripts`（导入后可批量把 Pending→Ready，减少热路径 ensure） |

**默认不在热路径 EnableNSFW**（只 set-birth）。开 NSFW 内容偏好仍走管理脚本 / 设置，避免每次请求改偏好；年龄前置足够降低「未成年拒 NSFW」。

**Console：** 默认仅 `ProviderWeb`。若 Console 与 Web **共用同一 SSO cookie 身份** 且上游仍按 grok.com 年龄策略拒，可配置 `console.ensureBirthDateViaLinkedWeb=true`，对 linked Web 账号执行同一套，**不要**假设 console.x.ai 有 set-birth API。

---

#### 落点

| 层 | 文件 | 改动 |
|----|------|------|
| Domain | `account.go` | 辅助 `func IsWebAdultReady(c Credential) bool` |
| Account | `web_account_settings.go` | `EnsureWebBirthDateOnce` 返回 (ready bool, err) |
| Gateway | `service.go` / `image.go` | Web 选号后、上游前：Pending→ensure；失败 exclude+continue |
| Selector | `selector.go` / plan | 可选：优先 AdultReady；attempt≥1 且已 ensure 失败过则 `requireAdultReady` |
| Config | `config.go` | `ensureBirthDateOnUse`（默认 true）、`adultPendingMaxEnsuresPerRequest=1` |
| 测试 | settings + gateway | 见验收 |

---

#### 无副作用

1. AdultReady 热路径 **零** 额外上游。  
2. 每请求对 Pending **最多 ensure 有限次**（默认 1 个号），防 set-birth 风暴。  
3. ensure 失败 → **换号**，不把错误当全局聊天失败（还有 Ready 号时）。  
4. 全池皆 Pending 且 ensure 全失败 → 明确错误（如 `no_adult_ready_account`），可观测。  
5. 不默认改 NSFW 偏好；不碰 Build OAuth。  
6. 写出后 egress 防重放、视频钉号等不变量不变。

---

#### 验收

1. Ready 号：请求前后上游 set-birth 调用次数 = 0。  
2. Pending 号：首次被选中 ensure 成功 → 库变 Ready → 同号后续请求 ensure 调用 = 0。  
3. Pending ensure 失败 → 同请求换到另一 Ready 号成功。  
4. 一请求内第二个 Pending **不再** ensure（若 maxEnsures=1），直接跳过或只选 Ready。  
5. AlreadySet 429 → 记 Ready，主请求继续。

---

### #5 T1 + T1b — no_image + 生图换号（P0，同波）

**目标：** 残号可聊；免费 imagine 失败可换号；后处理失败不换号。

#### T1 处理思路（完整链路）

```text
上游 1010 → 识别 → TagNoImage 持久化
         → 不按致命失败拉满 failure_count/指数冷却
媒体选号 → exclude tag
聊天选号 → 不排除
出图成功 → clear tag
```

**关键细节：**

| 点 | 约定 |
|----|------|
| 常量 | `account.TagNoImage = "no_image"` |
| 存储 | `provider_accounts.tags` JSON/TEXT 可空；迁移可逆 |
| 识别 | body/错误串 `systemErrCode` 大小写不敏感，int/string；集中函数 `IsAccountRiskImageError` |
| 选号 API | `Acquire(..., excludeTags []string)`；`image.go` 传 `[]string{TagNoImage}`；chat 不传 |
| 清标 | 仅**上游生成成功**后；下载失败不清也不打 1010 |

#### T1b 处理思路（修正 origin 过保守）

现状：`image.go` 中 `execute()` 返回 err 多数 **直接 return**。  
目标：与 chat 类似，在 `maxAttempts` 内：

| 结果 | 动作 |
|------|------|
| 1010 / 可 SWITCH 上游失败 / 传输失败 | exclude 当前号，`continue` |
| SSO 401 | reauth + continue（已有） |
| 429 额度窗 | 既有 reconcile + continue（已有） |
| `MediaPostProcessingError` | **return**，不换号、不 MarkFailure 指数 |
| 硬失败 body（明确不可重试） | return |

**无副作用：**

- **不**改变 `video.go` 钉号语义。  
- 换号不重放「已成功计费的生成」；仅未成功的 attempt。  
- tags 不影响非媒体路由。

**验收：**

1. 1010 → 有 tag；chat 可选中；image 跳过。  
2. 成功出图 → tag 清除。  
3. 连续两个号 generate 失败 → 第三次成功（mock）。  
4. 存盘失败 → 不选下一号 regenerate。

**测试文件建议：** `failure_test`、`image` 相关、`selector_test`、repository tags。

---

### #6 T3 — 非流动态超时（P0）

**处理思路：**

```text
if streaming {
  使用现有固定/空闲超时  // 零变化
} else {
  total = clamp(base + est_in/1000*rate + out_budget/1000*out_rate + reasoning, min, max)
  ctx deadline = total
}
```

**关键细节：**

- 包：`backend/internal/pkg/chattimeout`。  
- `max` 默认对齐现 `provider.*.chatTimeout`。  
- token 估计：轻量启发式即可，禁止重模型依赖。  
- 超时分类：继续走 `newTransportUpstreamFailure` / 非 AccountScoped。

**无副作用：** 流式路径字节级行为不变（单测或对比配置）。  
**验收：** 短请求 total 近 min；长 prompt 增大且 ≤max；流式 deadline 与改前一致。

---

### #7 T4 — 冷却分型（P0，单独 PR）

**处理思路：**

1. 引入 `FailureClass`（或扩展现有 failure 字段）：`Transport` / `TransientUpstream` / `RateLimitWindow` / `ModelDenied` / `CredentialDead` / `AccountRiskSoft` 等。  
2. `MarkFailure` 改为按 class：  
   - Transport / Transient：**cooldown=0**，仅换号  
   - RateLimitWindow：**模型或窗级**（既有 Mark*Quota），非账号指数  
   - CredentialDead：reauth  
   - AccountRiskSoft（1010）：只 tag  
3. **删除/绕过** `retryAfter > cooldown` 对账号级冷却的抬升（额度路径不走此分支）。  
4. `routing.cooldownMode=class|legacy`：`legacy` 恢复 30s 指数，便于回滚。

**无副作用：** 默认 `class` 会提高瞬时上游故障时的换号频率——符合免费池；用 legacy 可回滚。  
**验收：** 5xx 一次后账号仍可选；legacy 下行为接近旧版。

---

### #8 T5 — reauth 原因可运营（P1）

**处理思路（通用、少副作用）：**  
保持 `AuthStatus` 仍为 `active|reauthRequired`（少改 SQL check），新增 **`ReauthReason`** 字符串/枚举：`sso_rejected|refresh_permanent|expired|banned|...`。  
选号：非 active 仍全排除。  
UI：徽章映射 BANNED/EXPIRED 文案。  
自动清理：可按 reason 不同 minAge（二期）。

**无副作用：** 路由热路径只多读一个字段；无 reason 时 UI 显示通用 reauth。

---

### #9 H2 — CLI 推理头配置化（P1）

**处理思路：** `ClientIdentifier` / `ClientMode` / `UserAgent` / 可选 `CompactionAt` 全部配置化；**默认值 = 当前 origin 硬编码**（如 grok-shell、headless、0.2.106 UA）。  
**无副作用：** 默认指纹零变化；抓包对齐仅文档+可选配置档。  
**验收：** 现有 `adapter_test` 头断言仍过。

---

### #10 T6 — 近分 jitter（P1）

**处理思路：** 保留 10 维主序；仅在「关键比较键相等」时对连续分加 `hash(accountID, salt) * ratio`（ratio 默认 0.1，0 关闭）。  
salt 可用 request_id 或进程小时桶，保证可测（单测固定 salt）。  
**无副作用：** `ratio=0` 全序与旧确定性一致。

---

### #11 T7 — shape + out_bytes（P1）

**处理思路：** `jsonshape.KeyTree` 限深限宽；仅在解析失败/形状不匹配时 Warn；timing 增 out_bytes。  
**无副作用：** 成功路径不打 body；键树截断防日志爆炸。

---

### P2 摘要

| ID | 思路 |
|----|------|
| T8 | gateway switch/done/fail Debug，字段 request_id/account/attempt/reason |
| O2 | 可恢复 refresh 失败入现有 scheduler 队列，不新建循环体系 |
| T9 | 不做 |

---

## 4. 目录与符号锚点（origin）

```text
backend/internal/
  domain/account/account.go           # Tags, ReauthReason, TagNoImage
  application/gateway/
    service.go                        # chat attemptLoop, maxAttempts
    image.go                          # 生图 attempt 环（T1b）
    video.go                          # 钉号；禁止生成换号
    selector.go                       # Acquire/MarkFailure/excludeTags
    selector_plan.go                  # 堆序 + jitter
    failure.go                        # class / 1010 识别入口之一
    timing.go                         # out_bytes
  application/accountsync/service.go  # T2
  application/account/service.go      # convert / MarkReauth
  application/account/web_account_settings.go  # W1 生日/NSFW（已有，扩展 Ensure*）
  infra/provider/cli/oauth.go         # H1
  infra/provider/cli/adapter.go       # H2 applyHeaders
  infra/provider/web/sso_build.go     # H1/O1
  infra/provider/web/image.go         # 1010 上游形态 / 后处理重试
  infra/provider/browserheaders/      # Client Hints
  infra/egress/sticky_retry.go        # 写出不重放（勿动语义）
  infra/persistence/relational/       # tags/reason 迁移
  infra/config/config.go              # 开关与默认
  pkg/chattimeout/                    # T3 新建
  pkg/jsonshape/                      # T7 新建
frontend/src/features/accounts/       # T5 展示
```

**配置锚点（建议名，实现时可等价）：**

| 键 | 默认 | 任务 |
|----|------|------|
| `routing.maxAttempts` | 3（已有） | 全局 |
| `routing.cooldownMode` | `class` | T4 |
| `routing.selectionJitterRatio` | `0.1` | T6 |
| `batch.importSyncMaxRounds` | `5` | T2 |
| `chat.timeoutDynamic` | `true` | T3 |
| `provider.build.clientIdentifier` 等 | **现状值** | H2 |

---

## 5. 每任务门禁

1. 只改任务列出的文件（+ 必要测试/迁移）。  
2. `go test` 触及包通过。  
3. §0.3 不变量全绿。  
4. 新增日志走脱敏；无 cookie/token。  
5. DB 迁移可空默认。  
6. T4/H2/T3 流式：默认与回滚策略在 commit message 写明。

---

## 6. Commit 切分

| # | Message | 任务 |
|---|---------|------|
| 1 | `feat(auth): endpoint header matrix for oauth and sso-build` | H1(+O1) |
| 2 | `feat(import): rate-limit retry and end-of-batch recheck` | T2 |
| 3 | `feat(web): lazy ensure adult birth-date once on use` | W1 |
| 4 | `feat(media): no_image tag and image generate account switch` | T1+T1b |
| 5 | `feat(timeout): dynamic deadline for non-stream requests` | T3 |
| 6 | `fix(routing): class-based cooldown defaulting to zero` | T4 |
| 7 | `feat(account): persist reauth reason for ops` | T5 |
| 8 | `feat(cli): make inference headers configurable` | H2 |
| 9 | `feat(routing): near-tie selection jitter` | T6 |
| 10 | `chore(obs): upstream shape preview and out_bytes` | T7 |

---

## 7. 文档关系

| 文档 | 职责 |
|------|------|
| `~/grok2api/移植目标.md` | 对比证据与价值 |
| `~/grok2api/对比结果.md` §一 | OIDC/CLI 头与 §1.3 勘误 |
| **本文** | **唯一执行 SSOT**（顺序/落点/验收/无副作用） |
| `~/grok2api/修改计划.md` | Python dev 自身演进，**禁止混用** |

争议：先改证据文档结论 → 再改本文 → 再改代码。

---

## 8. 一句话

> **先头与进池（H1→T2→O1），再 Web 年龄前置懒确保（W1），再免费池利用率（T1+T1b→T3→T4），再运维与打散（T5→H2→T6），最后可观测（T7）。**  
> **通用 class 策略 + 免费池默认参数；付费只额外守写出不重放、视频钉号、凭证真死。**  
> **年龄：每次请求前查库；仅 Pending 号 ensure 一次并回写，失败则换 Ready 号。**
