# origin 修改计划（CLI 号池分层调度 + 水位预热）

> **读者：** 任意编码模型 / 人类执行者。  
> **属性要求：** **完整、通用、明确、无副作用**。  
> **产品本质：** 号池调度（选号 → 调用 → 即时反馈）；OpenAI/Anthropic 是外壳。  
> **策略 SSOT：** 同仓 `号池调度.md`（分层选号 + 分层预热 + 通道隔离 + 三级水源）。  
> **工程 SSOT：** 本文（库表 / 配置 / 代码落点 / 任务序 / 验收）。  
> **OIDC/抓包 SSOT：** `抓包分析.md`；C0 已完成项见 §12，本文不重做。  
> **基线分支：** `dev`。

---

## 实现进度（滚动）

| 任务 | 状态 | 说明 |
|------|------|------|
| P1 `build_cli_profiles` + repo + ListRoutingCandidates | **完成** | 表/CRUD/联表；Web 不装载 |
| P2 `ClassifyCLI` 纯函数 + 单测 | **完成** | `domain/account/cli_layer*.go` |
| P3 `routing.cli` 配置 | **完成** | config + example yaml；`UpdateCLISelect`/`SetCLIRouting` |
| P4 selector 硬分层 | **完成** | eligibility 过滤 + hard layer；preferFree 仅层内 |
| P5 成功/失败写 profile | **完成** | success + 403→maybe_dead + 402/429→CLI cooldown；Web 不写 profile |
| P6 warm worker | **完成** | 双阶段：soft floor 优先 + deficit 按 fill_order；RT 先于 Convert；unproven cap；原料夹逼 |
| P7 RT 死 → 异步 Convert | **完成** | 有 link 则 `EnqueueBuildCLIConvert`；`MaxConvertInflight` 并行 + `MaxConvertPerMinute`；不连坐 Web reauth |
| P8 due 刷新收敛 | **完成** | Build due 仅 proven/trusted/super 或 AutoFillUnproven |
| P9 指标 | **完成** | 选号层计数 + warm snapshot + 账号详情 cli* + routing_decision cli_layer |
| P10 文档 | **完成（对照用）** | `号池调度.md` §14 实现落点；细节验收仍由人对照 |

---

## 0. 目标与标尺

### 0.1 要解决什么

| 缺口 | 说明 |
|------|------|
| CLI 选号无硬分层 | 未吸收 Python「非免费∧成功 > 成功 > 信任未证 > 未知 > bot」 |
| 预热无优先级 | `RunCredentialRefresh` 偏「due 就刷」，易对 basic 海货无脑 OAuth |
| 成功证明非选号主轴 | 无稳定 `last_success` 驱动层 |
| RT 死仅 reauth/换号 | 缺「旁路 Convert 补水位」且不伤 Web |
| 配置无水位/层目标 | `routing` 仅有 jitter/cooldown，无 CLI warm 段 |

### 0.2 四条标尺（实现自检）

| 标尺 | 检查问句 |
|------|----------|
| **完整** | 选号过滤、硬分层、层内分、预热选料、RT、Convert、废弃、指标是否都有写/读/测？ |
| **通用** | 层函数是否纯函数可测？非免费真值是否单一？Web/Console 是否零调用？ |
| **明确** | 字段名、配置默认、任务 ID、禁止副作用是否写死？ |
| **无副作用** | CLI 写路径是否只碰 Build？Convert 是否默认同请求？basic 未证是否默认不 Convert 预热？ |

### 0.3 不变量（禁止破坏）

1. **通道隔离：** CLI refresh/convert/分层状态 **只** 影响 `grok_build`；禁止改 Web/Console 的 auth/cooldown/quota。  
2. 传输超时 → 不 `MarkFailure` 长冷却。  
3. sticky / previous_response → chat attempts=1；视频 generate **钉号**。  
4. egress：`WroteRequest` 后禁止重放 POST。  
5. 日志不落 cookie / refresh_token / 完整 access。  
6. Convert **不**作 6h 日常续命；请求 **默认不同步** 10–15s Convert。  
7. 不做整包 PKCE；不做每请求 set-birth-date。  
8. **预热按层补水位**，禁止「任意 ready 凑满 N」挤掉 proven。  
9. C0 头/JWT/bot 双语义保持；Convert 污染门闩与运行时 Super bot **不混用**。

### 0.4 采纳结论（相对 Python）

**CLI 调度与预热选择：吸收 Python 双轴模型（更先进）。**  
**存储与通道：保持 origin（分 Provider + 加密 + link）。**

---

## 0.5 分层落点与追踪（与 `号池调度.md` §3.6–3.8 一致）

| 项 | 确认 |
|----|------|
| 层计算 | **唯一** `domain/account.ClassifyCLI` 纯函数；选号与预热共用 |
| 层存储 | **不**落 `cli_layer` 枚举列；每次现算 |
| 事实存储 | `build_cli_profiles`（last_success / trusted / CLI 冷却 / generation…） |
| 选号接入 | `selector.Acquire` 仅 `ProviderBuild`：eligibility → **硬分层** → 层内 score |
| 预热接入 | `cli_warm_worker` 同函数 + fill_order + unproven cap |
| 装载 | `ListRoutingCandidates(Build)` 批量挂 profile，SQL 不算层 |
| 追踪 | 指标 `cli_select_layer` / `cli_ready_*` / `cli_warm_fill_*`；日志带 layer= |
| 回滚 | `routing.cli.enabled=false` 跳过硬分层与 worker |

---

## 1. 数据库：是否修改？

### 1.1 结论

| 项 | 要不要改 | 说明 |
|----|----------|------|
| **需要** 增加 **Build CLI 运营画像表**（或等价列） | **是** | 成功证明 / 信任 / CLI 冷却 / 调用计数等必须可索引、可增量写，禁止再塞 fat `ext` |
| 改 `account_credentials` 主语义 | **否** | access/refresh/expires/refresh_due 已够 RT 续命 |
| 改 Web profile / Web 表 | **否** | 无副作用 |
| 为 eligibility 存永久枚举列 | **否（推荐）** | **运行时推导**（与 Python 从 ext 推导一致），避免双源不一致 |
| 账号 soft-delete / pool 分片列 | **本期否** | 非本计划必需；分层用 Super/成功证明表达 |

### 1.2 新增表：`build_cli_profiles`（推荐）

与 `web_account_profiles` 对称：**仅** `provider=grok_build` 账号 1:1。

| 列 | 类型 | 含义 |
|----|------|------|
| `account_id` | PK, FK → `provider_accounts.id` ON DELETE CASCADE | Build 账号 |
| `last_success_at` | DATETIME NULL | **成功证明**；非空 ⇒ proven（层 1/2） |
| `success_count` | INT NOT NULL DEFAULT 0 | 诊断 + 可选打散 |
| `call_count` | INT NOT NULL DEFAULT 0 | 层内 `call_count` 降权 |
| `trusted_source` | BOOL NOT NULL DEFAULT false | 层 3 |
| `maybe_dead` | BOOL NOT NULL DEFAULT false | 连续 403 等 soft；助 BOT 层 |
| `consecutive_403` | INT NOT NULL DEFAULT 0 | 层内惩罚 / maybe_dead 阈值 |
| `next_eligible_at` | DATETIME NULL | CLI 专用冷却（402/429）；**不是** Web cooldown_until |
| `token_generation` | INT NOT NULL DEFAULT 0 | Convert 成功 +1；旧 RT 世代 |
| `last_cli_error_code` | VARCHAR(64) NOT NULL DEFAULT '' | 运维 |
| `updated_at` | DATETIME NOT NULL | |

**索引：**

- `(last_success_at)` 部分：WHERE last_success_at IS NOT NULL（可选）  
- `(next_eligible_at)`  
- 预热选料可用 join `provider_accounts` + `account_credentials`

**非免费（层 1 条件之一）：不新增列**，复用已有：

- `provider_accounts.build_super_entitled`  
- 和/或 `account_billing_snapshots` 付费信号  
- 统一函数：`account.IsBuildSuper(cred, billing)`（已有）

**Bot 运行时 flag：** 继续 JWT/`BuildBotFlagged` 元数据路径；`maybe_dead` 为 soft，与 convert 污染门闩分离。

### 1.3 推导字段（不落库）

```text
cli_eligibility =
  DENIED         if maybe_dead 持久策略到顶 / 无 scope 类（若有）…  // 实现期列白名单
  TEMP_BLOCKED   if next_eligible_at > now
  READY          if access 非空 && expires_at > now+skew
  REFRESHABLE    if refresh 非空 && !READY
  ACQUIRE_ALLOWED if linked Web SSO 可读 && 允许 convert
  BOT_FLAGGED    if maybe_dead || BuildBotFlagged（无 success 时进层 5）
```

`cli_layer`（纯函数，选号与预热共用）：

```text
1 if IsBuildSuper && last_success_at != null
2 if last_success_at != null
3 if trusted_source && !success
5 if (maybe_dead || bot) && !success
4 else
```

### 1.4 迁移

- GORM AutoMigrate / 现有 `schema.go` 注册 model。  
- 启动 backfill：为所有 `grok_build` 账号插入空 profile 行（全默认）。  
- **不**从 Python `ext` 自动迁（另开迁移工具）；可选手动 SQL 导入 `cli_last_success_at` 等。

### 1.5 明确不建

- 不把 CLI 状态写进 `web_account_profiles`。  
- 不恢复账号级 fat JSON `ext`。  
- 不为「预热队列」建持久 job 表（先用内存 worker + DB 查询；失败靠 due/标记重试）。

---

## 2. 配置项：有无变化？

### 2.1 结论

**有变化：** 新增 `routing.cli`（或顶层 `cliPool`）配置段；**现有** `routing.selectionJitterRatio` / `cooldownMode` 等保留。  
**默认：** 总水位为主；未证 **参与** 凑水位但有 cap；禁止无序全池 Convert。

### 2.2 建议 YAML（`config.example.yaml` + `RoutingConfig` 扩展）

```yaml
routing:
  # …既有 stickyTTL / cooldown* / selectionJitterRatio …
  cli:
    enabled: true
    # 主目标：各类「可能能用」READY 加总
    warmTargetTotal: 500          # 20K 池示例；小池 100~300
    warmLowWatermarkRatio: 0.8
    warmFillOrder: [l1, l2, nonfree_unproven, l3, l4]
    # 软地板：effective = min(floor, bucket 可用原料)；禁止为追 floor 无界 Convert
    warmSoftFloorL1: 20            # 20K 池若仅 2 非免费 → 实际只追 2
    warmSoftFloorL2: 50
    warmSoftFloorL3: 10
    warmSoftFloorL4: 20            # 非 proven 也保持一些
    warmSoftFloorNonFreeUnproven: 10
    # 未证参与水位的上限；target=500 → cap=min(0.4*500,120)=120
    warmMaxUnprovenShare: 0.40
    warmMaxUnprovenAbs: 120
    autoFillNonFree: true
    autoFillUnproven: true          # 非 proven 也参与凑 total（有界 Convert/RT）
    autoFillRequireLinkedWeb: true
    accessRefreshAdvance: 3m
    maxRefreshInflight: 25
    maxConvertInflight: 3
    maxConvertPerMinute: 20
    warmTickInterval: 15s
    convertOnRequest: false
    layerHardPartition: true
    # 选号集合：默认不含仅 ACQUIRE（无 token）的号，避免请求同步 Convert
    selectReadyOrRefreshableOnly: true
    callCountWeight: 50
    recordSuccessOnOK: true
```

### 2.3 配置映射（代码）

| YAML | Go 字段（建议） | 默认 |
|------|-----------------|------|
| `routing.cli.*` | `config.RoutingConfig.CLI CLIRoutingConfig` | 见上 |
| 校验 | `warmTargetTotal > 0`；unproven cap 一致；share∈[0,1] | 非法则安全默认 |

### 2.4 行为开关矩阵

| 开关 | false 时 |
|------|----------|
| `cli.enabled` | 保持现有选号/全 due refresh，不分层（兼容回滚） |
| `autoFillUnproven` | 未证不主动 Convert/RT 凑水位（仅 proven） |
| `convertOnRequest` | 请求路径只 RT + 换号 |

---

## 3. 代码如何实现

### 3.1 模块落点（最小新面）

```text
backend/internal/domain/account/
  cli_layer.go          # CliLayer / CliEligibility 纯函数 + 单测
  cli_profile.go        # 领域结构体（可选与 Credential 组合）

backend/internal/infra/persistence/relational/
  models.go             # buildCLIProfileModel
  schema.go             # 注册
  build_cli_profile_repository.go  # Get/Upsert/RecordSuccess/RecordError
  account_repository.go # ListRoutingCandidates JOIN profile（仅 Build）

backend/internal/application/gateway/
  selector.go           # Build 路径：硬分层 + 层内分；Web/Console 不动
  selector_cli_layer_test.go

backend/internal/application/account/
  cli_warm_worker.go    # 水位循环：按层 RT → Convert
  service.go            # EnsureCredential / RT 失败分类 → 标记 need convert
  record_cli_outcome    # 网关成功/403/429 写 profile

backend/internal/app/
  application.go        # 启动 RunCLIWarmWorker；收敛 RunCredentialRefresh 与 warm 关系
```

### 3.2 核心算法（必须共用）

```go
// 伪代码 — 选号与预热必须调用同一实现
func ClassifyCLI(c Credential, p CLIProfile, billing *Billing, now time.Time) (elig Eligibility, layer int)

func OrderCLICandidates(items []RoutingCandidate, now time.Time) []RoutingCandidate {
  // 1) filter elig 可选 && next_eligible <= now
  // 2) stable partition by layer 1..5
  // 3) within layer: score + optional jitter
}
```

**无副作用：** `OrderCLICandidates` 仅在 `provider == grok_build` 调用；Web 仍走现有 score。

### 3.3 选号（请求路径）

```text
Selector.Acquire(Build):
  loadCandidates（含 profile + billing）
  OrderCLICandidates
  claim slot…

Gateway 成功（Build）:
  RecordCLISuccess(accountID)  // last_success_at, success_count++, clear 403/maybe_dead
  不写 Web

Gateway CLI 403/429:
  RecordCLIError → consecutive_403 / next_eligible_at / maybe_dead
  仅 Build profile

401:
  EnsureCredential(force=true)
  若 RT permanent → 不 MarkReauth Web；Build 标 convertible；本请求换号
  WakeCLIWarm
```

### 3.4 预热 Worker（旁路）

```text
RunCLIWarmWorker:
  tick / Wake:
    total = CountReady(L1+L2+L3+L4+nonfree_unproven)  // 可能能用的加总
    if total >= warmTargetTotal * lowRatio: return

    deficit = warmTargetTotal - total
    for bucket in fillOrder:  // l1 → l2 → nonfree_unproven → l3 → l4
      if deficit <= 0: break
      if bucket unproven && unprovenReady >= cap: continue
      // 先满足 soft floor，再贡献 total
      pick REFRESHABLE → RT
      else if autoFill* && linked Web: Convert（限流）
      成功 → deficit--
    L5 skip
```

**与现有 `RunCredentialRefresh` 关系（明确）：**

| 方案 | 选择 |
|------|------|
| **推荐** | Warm **接管** Build 缺口驱动；due 列表经「是否有助于 total / soft floor」过滤，禁止无脑全池 due |
| 禁止 | 启动对 20K 全量 Convert |
| 允许 | 启动 warm tick 直到 total 达标或原料尽 |

### 3.5 RT 真死 → Convert（无副作用细则）

```text
recordCredentialRefreshFailure:
  permanent RT 死:
    Build: RefreshPermanent / profile 可标
    若有 linked Web: 入 convert 候选（warm 消费）
    禁止: MarkReauth(Web) / disable Web / Web cooldown
  SSO Convert 失败且 SSO 真死:
    才 MarkReauth(Web) 或废 link 策略（产品可选；默认 Build reauth + 解绑）
Convert 成功:
  UpdateTokens Build
  token_generation++
  同 SSO 其它 Build（若有）: 旧 generation 失效或强制 reauth Build
  持 per-SSO 锁（Redis key sso-convert:{egress_identity|web_id}）
```

### 3.6 非免费真值（单一）

```text
IsNonFreeBuild(c, billing) := account.IsBuildSuper(c, billing)
```

文档与单测锁死；禁止第二套 pool 字符串散落。

### 3.7 测试（完整）

| 测试 | 断言 |
|------|------|
| `cli_layer_test` | 五层矩阵（Super+success / free success / trusted / plain / bot） |
| `selector_cli_layer_test` | 有 L1 时不选 L4；层内 call_count 打散 |
| `cli_warm_order_test` | total 不足时先 L1/L2 再未证；未证受 cap；加总达 target 即停 |
| `isolation_test` | RecordCLIError 后 Web ListRouting 冷却不变 |
| `refresh_fail_no_web_reauth` | RT permanent 不写 Web reauth |
| 回归 | 既有 gateway/selector/credential_refresh 测试全绿 |

### 3.8 指标（明确）

- `cli_ready_layer{layer=1..5}`  
- `cli_select_layer`  
- `cli_warm_refresh_total` / `cli_warm_convert_total`  
- `cli_warm_deficit{layer}`  
- 日志：`cli_warm` / `cli_layer` 不含密钥  

---

## 4. 任务拆分与执行顺序

### 4.1 任务表

| ID | 内容 | 依赖 |
|----|------|------|
| **P1** | DB：`build_cli_profiles` model + migrate + repo | — |
| **P2** | domain：`ClassifyCLI` / `CliLayer` 纯函数 + 单测 | — |
| **P3** | config：`routing.cli` 结构、example、校验 | — |
| **P4** | ListRoutingCandidates / 选号：Build 硬分层 + 层内分 | P1 P2 P3 |
| **P5** | 网关：成功/403/429 写 profile；401 只动 Build | P1 P4 |
| **P6** | Warm worker：按层 RT；缺口驱动；限流 | P1 P2 P3 |
| **P7** | RT permanent → convert 候选 + 异步 Convert + SSO 锁；成功 generation | P6 |
| **P8** | 收敛 `RunCredentialRefresh` 与 warm（过滤/优先级） | P6 |
| **P9** | 指标 + 管理 API 只读展示层/ready（可选） | P4 P6 |
| **P10** | 文档：`号池调度.md` 与实现对齐检查；README 链到配置 | 全部 |

### 4.2 执行顺序（复制即用）

```text
1  P1  表与 repo
2  P2  纯函数分层（可与 P1 并行）
3  P3  配置
4  P4  选号接入
5  P5  反馈写 profile
6  P6  水位 RT 预热
7  P7  Convert 补水（有界）
8  P8  旧 refresh 调度收敛
9  P9  观测
10 P10 文档收口
```

### 4.3 每任务无副作用清单

| 任务 | 禁止 |
|------|------|
| P1 | 改 Web 表结构 |
| P4 | 改 Web/Console score 语义 |
| P5 | 成功写 Web last_used 当 CLI 成功（可用 Build last_used） |
| P6 | 启动全池 Convert；basic 未证默认 Convert |
| P7 | 请求同步 Convert；Convert 失败 MarkReauth Web（除非 SSO 真死策略显式开启） |
| P8 | 删掉 EnsureCredential 热路径 RT（请求仍要快路径） |

---

## 4.A 组成关系验收样例（20K / 水位 500）

> 策略细节见 `号池调度.md` §7；此处作工程验收口径。

**货源假设：** 非免费 2 · Proven 400 · Trusted 2500 · 无效 2000 · 其余≈15098 无标志/bot。

| 检查项 | 期望 |
|--------|------|
| unproven_cap | `min(0.4×500, 120)=120` |
| soft floor L1 | 原料仅 2 → effective_floor=2，不空转 |
| 稳态 warm 组成（proven 全 READY） | L1=2 + L2≈398 + L3≈100 + L4=0 = **500** |
| 未证占比 | 100/500=20% ≤ share 与 abs |
| L5 bot / 无效 | **不进** warm 计数 |
| proven_READY 仅 200 时 | max_ready≈320 **&lt; 500** → 水位告警，**禁止**靠 L4 海货 Convert 硬凑满 |
| 选号 | warm 可含 L3；有 L1/L2 可选时 **不得** 先出 L3 |

**实现公式（worker）：**

```text
unproven_cap = min(floor(warmTargetTotal * share), abs)
effective_floor(b) = min(softFloor(b), countMaterial(b))
max_achievable ≈ proven_READY + unproven_cap   // 告警用
```

---

## 5. 验收

### 5.1 功能

- [ ] Build 选号：存在 L1 时日志/指标显示不选 L4 未证。  
- [ ] 首次 CLI 成功后该号进入 L2（免费）或 L1（Super）。  
- [ ] total 不足时未证可参与补水；达 unproven cap 后不再 Convert 未证。  
- [ ] total 达标后 worker 空闲（仅 soft floor 维护可选）。  
- [ ] RT 刷新只更新 Build credentials + 可选 profile 时间戳。  
- [ ] Web 请求路径零 Convert/零 build_cli_profiles 写入。  
- [ ] RT permanent：Build 可异步 Convert；Web 账号仍可被 Web 路由选中（隔离）。  

### 5.2 命令

```text
go test ./backend/internal/domain/account/ ...
go test ./backend/internal/application/gateway/ ...
go test ./backend/internal/application/account/ ...
go test ./backend/internal/infra/persistence/relational/ ...
```

### 5.3 标尺复核

| 标尺 | 满足方式 |
|------|----------|
| 完整 | P1–P8 覆盖选号+预热+RT+Convert+反馈 |
| 通用 | 纯函数分层；IsBuildSuper 单一非免费源 |
| 明确 | 本文表/配置/任务 ID |
| 无副作用 | §0.3 + 每任务禁止表 + isolation 测试 |

---

## 6. 不做清单（本期）

- Python `accounts.db` 整库迁移工具（可另开）。  
- fat `ext`、账号 soft-delete、全局 pool 列。  
- 请求默认同步 Convert；流式动态墙钟；视频换号。  
- 重做 C0 OIDC 头矩阵（已完成）。  
- 用 Convert 替代 RT 日常续命。  
- 预热「只凑 total ready、无视层」。  

---

## 7. 风险与回滚

| 风险 | 缓解 |
|------|------|
| 分层过狠导致 L1 耗尽无号 | L2 回落；maxAttempts 换号；指标告警 |
| Convert 打爆公开 SSO | maxConvertInflight/min；unproven share/abs cap |
| 写 profile 拖慢热路径 | 成功写异步或批量；失败不影响响应 |
| 行为突变 | `routing.cli.enabled=false` 回退旧选号 |

---

## 8. 文档关系

| 文档 | 职责 |
|------|------|
| **`号池调度.md`** | 策略：层、水位、隔离、水源 |
| **本文 `修改计划.md`** | 工程：库、配置、代码、任务、验收 |
| `抓包分析.md` | OIDC/JWT 事实 |
| 旧 C0 段落 | 见 §12；OIDC 已闭环 |

---

## 9. 实现备忘（关键细节）

### 9.1 ListRoutingCandidates

- Build：`Preload`/二次查询 `build_cli_profiles` + billing。  
- 候选缓存 1s：profile 更新后 `invalidateCandidates(Build)`。  
- **禁止**为分层 preload Web SSO 明文到选号热路径（Convert 时再解密）。

### 9.2 EnsureCredential

- 保持 3m advance；成功 UpdateTokens 后刷新 `refresh_due_at`。  
- permanent 失败：只 Build；`WakeCLIWarm`。

### 9.3 Convert 与 generation

- 成功：`token_generation++`；同 `egress_identity` 其它 Build 若 generation 旧则拒绝 refresh 或标 reauth **Build-only**。

### 9.4 与 PreferFreeBuild

- 既有 `preferFreeBuild` 与层 1 Super 可能冲突：  
  **明确规则：** `layerHardPartition` 优先；层内再应用 preferFree / Super 排序。  
  写入 selector 注释 + 测试，避免双策略互撕。

---

## 10. 一句话

**库加 `build_cli_profiles`；配置 `routing.cli` 以 `warmTargetTotal` 为主（各类可能能用 READY 加总），`fillOrder` 决定缺水时先补谁（含非 proven）；选号仍硬分层。先 RT 后有界 Convert；只写 Build，Web 零串味。**

---

## 12. 附录：已完成工作（勿回滚）

### 12.1 C0 OIDC / Convert（已完成）

共享 `xaiauth`、CLIAuthForm、sso_build 双轨头、JWT 血缘与 convert bot 门闩、enrichment、refresh principal、429 backoff 等 — 以仓库 `dev` 提交与原 C0 清单为准。

### 12.2 池子任务（已完成，本文不重做）

`T2` 导入 429、`W1` 年龄门、`T1` no_image、`T3` 非流动态超时、`T4` class 冷却、`T5` reauth 原因、`H2` CLI 头、`T6` jitter、`T7` shape、`T8` debug、`O1`/`O2` 等。

### 12.3 与本文关系

C0 解决「CLI 凭证怎么像官方一样拿到/续上」；  
本文解决「拿到后 **谁先被选、养多少、RT 死了怎么旁路复活且不伤 Web**」。
