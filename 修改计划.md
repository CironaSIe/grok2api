> **读者：** 任意编码模型 / 人类执行者。  
> **质量标尺：** **完整、通用、明确、无副作用**。  
> **产品本质：** 号池（进货 → 出站 → 选号 → 反馈）；OpenAI/Anthropic 是外壳。  
> **策略 SSOT：** 同仓 `号池调度.md`（CLI 分层/水位/§4.3.1 开荒 / §4.7 explore）。  
> **工程 SSOT：** 本文（剩余工作 + 历史决策摘要）。  
> **对标：** `~/grok2api` 的管理端协议与写路径精神，**不是**存储 1:1。  
> **基线：** Q1–Q9、CLI P1–P10、C0 OIDC **勿回滚**。  
> **约束：** 未要求则不重启；端口 **8888**。

---

## 实现进度

| 任务 | 状态 | 说明 |
|------|------|------|
| **本期交付（W0–W5 + CLI 线）** | **完成** | 见下方「已交付摘要」；细节以代码与 `号池调度.md` 为准 |
| **§15 SSO2OAUTH 错误展示强化** | **待办** | 见 §15 |
| **R8** 双池逻辑库存 | **远期 RFC** | 本期不表合并；Web/Console 双行 + R3h 止血 |

### 已交付摘要（完成项已从待办清除）

| 波次 | 内容 |
|------|------|
| CLI | 403 一次 soft-dead + 池内换号；chat 403+JWT 活→封号出池；soft bot→L5 仍可选；explore 旁路验真（默认关） |
| W0 | ReconcileMany；Convert 去 Trusted 勾 |
| W1 | snapshot/revision 管理读 |
| W2 | writequeue；health coalesce；sync 并发；Console 投影 identity |
| W3 | 水位开荒 autoPioneer |
| W4 | 异步 taskId；pending_nsfw；TaskDock + 标签芯片 |
| W5-R6 | `allowNSFW` 时请求路径 `EnsureWebNSFWOnce`；短路/失败换号 |

> 已完成波次的详细设计（R3-0 snapshot 备选、R3a–h 写路径、R4/R5/R6/R7 等）已落地为代码，不再在本文保留备选矩阵。历史决策见 git log。

### CLI 语义（勿回退）

- **封号 `maybe_dead`** = `chat_banned`：**不可选**、不计 warm、Attention/UI「封号」；不 RT、不 reauth。  
- **soft bot** = 仅未证时 **L5**，Access 可用则仍可选（对齐 `~/grok2api` `_cli_layer`）。  
- **explore** = warm Phase E，默认 `exploreEnabled: false`；见 `号池调度.md` §4.7。

---

## 15. SSO2OAUTH 错误信息展示强化

> **触发：** 用户要求强化 SSO→Build 转换（sso2oauth）错误信息展示。
> **目标：** 失败原因（phase/status/URL/body 已在 err 文案）+ 分类标签，在任务面板与结果弹窗两处可查。
> **质量标尺：** 完整、通用、明确、无副作用。

### 15.1 现状与缺陷

| # | 位置 | 缺陷 | 影响 |
|---|------|------|------|
| D1 | `service.go:2077` `BuildConversionFailure` | 仅 `AccountID/Message`，无分类 | 前端无法按类（SSO失效/限流/网络/永久/bot）归类展示 |
| D2 | `service.go:2078` 失败日志 | 缺 `class` 字段（重试日志 2172 已有） | ops 排查时无法按类聚合 |
| D3 | `handler.go:1083` sync 整体失败 | `WriteError("…","Grok Web 账号转换失败")` 写死文案 | 丢弃底层 `sso2oauth[phase]…status=…` 细节 |
| D4 | `handler.go executeWebToBuildConversionTask` async 整体失败 | `task.Fail(err.Error())` 丢弃已收集的 `result.Failures` | 边界场景（非 ctx cancel）部分失败详情丢失 |
| D5 | `admin-task-dock.tsx` | 数据源 `ListActive`（仅运行中），完成后消失；无展开 | 任务完成即不可查失败详情 |
| D6 | `accounts-page.tsx buildConversionFailureMessage` | toast 仅前 3 个 + `+N more` | 大量失败被截断 |

> **非缺陷（已正确）：** async 整体失败 `task.Fail(err.Error())` 已存真实 err，前端 `pollAdminTaskResult` `status==="error"` 时 `throw snap.error`，toast 能看到 err 细节。D4 仅指 `result.Failures` 丢失。

### 15.2 方案选定（基于澄清）

| 维度 | 决策 | 依据 |
|------|------|------|
| 落点 | 前后端全链路 | 用户选"两者都要" |
| 展示位置 | 任务面板（含近期完成，可展开）+ 结果弹窗 | 用户选"两处都强化" + "dock 改为含近期完成" |
| 分类粒度 | 分类标签 + 原因文案（不单独抽 phase/status 结构化字段） | 用户选"分类标签+原因文案" |
| class 来源 | 复用现有 6 类 `ClassifyConversionError` | 已有测试 `sso_build_conversion_class_test.go` 覆盖 |

### 15.3 数据契约改动（向后兼容）

| 层 | 结构 | 改动 | 兼容性 |
|----|------|------|--------|
| service | `BuildConversionFailure` | 加 `Class string` | 应用层结构，加字段不影响调用方 |
| HTTP | `buildConversionFailureResponse` | 加 `Class string \`json:"class,omitempty"\`` | `omitempty`，旧前端不解析仍可用 |
| HTTP | `newBuildConversionResponse` | 传递 `Class` | 同上 |
| FE | `BuildConversionFailureDTO` | 加 `class?: string` | 可选字段 |
| FE | `decodeBuildConversionFailures` | 解析 `class`（`isOptional(isString)`） | 缺失即 undefined |
| FE | `buildConversionResultFromRecord` | 传递 `class` | 同上 |

**class 取值**：`sso_dead` / `rate_limited` / `network_retry` / `permanent` / `bot_contaminated` / `unknown`（`ClassifyConversionError` 已有，含 HTTP status + 错误文案双路判定）。

### 15.4 后端改动

| ID | 文件:行 | 改动 | 无副作用 |
|----|---------|------|----------|
| B1 | `service.go:2077` | 收集处 `Class: string(webprovider.ClassifyConversionError(item.err))` | 复用已有分类函数 |
| B2 | `service.go:2078` | 失败日志加 `"class", string(webprovider.ClassifyConversionError(item.err))` | 与 retry 日志 2172 一致 |
| B3 | `handler.go:1083` | sync 整体失败：`c.Request.Context().Err()!=nil` → `WriteError("accountConversionCancelled","转换已取消")`；否则 `WriteError("accountConversionFailed", err.Error())` | 区分取消，err 含 sso2oauth 细节 |
| B4 | `registry.go` | 新增 `FailWithResult(message string, result map[string]any)`：同 `Fail` 额外存 `result` | 新方法，不影响现有 `Fail` 调用 |
| B5 | `handler.go executeWebToBuildConversionTask` | async 整体失败（err 非 ctx.Err）：`result.Failures` 非空 → `task.FailWithResult(err.Error(), buildConversionTaskResultMap(payload))`；否则仍 `task.Fail(err.Error())`。新增 `buildConversionTaskResultMap(payload buildConversionResponse) map[string]any` 提取现有 `task.Finish` 内联 map（`1088-1091`），供 `Finish` 与 `FailWithResult` 共用 | ctx.Err 仍 `MarkCancelled`；`pollAdminTaskResult` 行为不变；`Finish` 改调同一 helper，行为等价 |
| B6 | `handler.go newBuildConversionResponse` | 传递 `Class` | 见 15.3 |

> **B5 边界说明：** async 整体失败时 `pollAdminTaskResult` 仍 `throw snap.error`（行为不变），`FailWithResult` 仅为 dock 通过 `ListRecent` 读取 `snap.result.failures` 提供数据。部分成功计数（created/linked）在整体失败语义下不展示，保持现状。

### 15.5 前端改动

| ID | 文件 | 改动 | 无副作用 |
|----|------|------|----------|
| F1 | `accounts-api.ts` | `BuildConversionFailureDTO` 加 `class?: string`；`decodeBuildConversionFailures`/`buildConversionResultFromRecord` 传递 | 见 15.3 |
| F2 | `accounts-api.ts` | 新增 `listRecentAdminTasks(signal)` → `GET /api/admin/v1/tasks/recent`，内部 `value.tasks.map(decodeAdminTaskSnapshot)`（与 `listActiveAdminTasks` 同模式） | 复用已有端点；`listActiveAdminTasks` 仅 dock 调用，切换后可移除或保留备用（无其他调用方） |
| F3 | `accounts-api.ts` | 导出 `decodeBuildConversionFailures`（或新增 `decodeAdminTaskFailures` 包装），供 dock 解析 `snap.result.failures` | **不改** `decodeAdminTaskSnapshot` 的 `result` 字段定义（仍 `isOptional(isRecordOf(() => true))`），不影响其他 task 类型 |
| F4 | `admin-task-dock.tsx` | 数据源改 `listRecentAdminTasks` | `/tasks/recent` 已存在 |
| F5 | `admin-task-dock.tsx` | refetch 逻辑：运行中 1500ms / 仅完成态 8000ms / 全空 `false` 停止；完成态任务依赖 registry TTL 30min 自动从 `ListRecent` 消失，dock 逐渐清空 | 避免对完成态高频轮询；无需手动清除机制 |
| F6 | `admin-task-dock.tsx` | `TaskRow`：已完成且 `fail>0` 可展开 failures 列表（`class` Badge + accountId + message） | 运行中无 `result`，不展示展开；完成态 `fail=0` 不展开 |
| F7 | `accounts-page.tsx` | 新增结果弹窗（`Dialog`）：convertWebAccounts mutation onSuccess `failed>0` 时 `setConversionFailures(result.failures)` 并开弹窗；弹窗读 state 列全部 failures + class 标签 + 原因 | `failed=0` 不弹，success 路径不变 |
| F8 | `accounts-page.tsx` | `buildConversionFailureMessage` 保留（toast 简略） | toast 与弹窗分工：toast 摘要，弹窗全量 |
| F9 | `i18n/index.ts` | 中英双语补 class 标签（6 类）、弹窗标题/列头/关闭/复制、`apiErrors.accountConversionCancelled` | 新增 key，不改动现有 |

**class 标签文案**：`sso_dead`=SSO失效 / `rate_limited`=限流 / `network_retry`=网络重试 / `permanent`=永久失败 / `bot_contaminated`=Bot标记 / `unknown`=未知。

**Badge variant 映射**（基于 `badge.tsx` 现有 4 variant）：

| class | variant | 理由 |
|-------|---------|------|
| `sso_dead` / `permanent` / `bot_contaminated` | `destructive` | 需人工介入，红色警示 |
| `rate_limited` | `secondary` | 暂时性，可重试 |
| `network_retry` | `outline` | 瞬态，边框轻量 |
| `unknown` | `default` | 中性 |

**弹窗**：标题"转换失败 {{failed}} 个账号"；列「账号 / 分类 / 原因」；可关闭、可复制全部（便于反馈）。

### 15.6 测试

| ID | 文件 | 内容 |
|----|------|------|
| T1 | `sso_build_conversion_class_test.go` | 已覆盖 `ClassifyConversionError`，**不改** |
| T2 | `conversion_test.go` | `235-239` 断言 `Failures[0].AccountID/Message`，补 `Class` 期望。注意：`229` 的 mock err 是纯 `fmt.Errorf`（未包装 `conversionHTTPError`），`ClassifyConversionError` 走字符串兜底返回 `unknown`（文案虽含 `status=403` 但分类器无 403 关键字），故期望 `Class == "unknown"` |
| T3 | `registry_test.go` | 新增 `FailWithResult`：存 result + status=Error；已 Done/Cancelled 不覆盖 |
| T4 | handler | `newBuildConversionResponse` 传递 `Class` 断言（若现有测试） |
| T5 | FE | decoder `class` 解析；`listRecentAdminTasks` 路径（若有测试基建） |

### 15.7 无副作用清单

| 做 | 不做 |
|----|------|
| `class` 字段 `omitempty` 向后兼容 | 破坏旧前端解析 |
| `/tasks/recent` 复用已有端点 | 新增后端端点 / 改 `/tasks` 语义 |
| `FailWithResult` 新方法独立 | 改 `Fail` 签名影响所有调用方 |
| sync `WriteError` 区分 ctx cancel | 泄露 context 内部细节给前端 |
| 结果弹窗仅 `failed>0` 出现 | 破坏 success 路径 toast |
| dock refetch 完成态降频 | 对完成态高频轮询 |
| 复用现有 `ClassifyConversionError` | 重新发明分类逻辑 |
| 日志 `class` 与 retry 日志一致 | 引入新日志格式 |
| F3 不改 `decodeAdminTaskSnapshot.result` | 影响其他 task 类型解码 |

### 15.8 验收

| 场景 | 标准 |
|------|------|
| 部分失败 | toast 摘要 + 弹窗列全部失败（class 标签 + 原因） |
| 整体失败（sync） | toast 含 `sso2oauth[phase]…` err 细节 |
| 整体失败（async） | toast 含 err；dock 该任务可展开看已收集 failures |
| 任务面板 | 完成态转换任务可见，`fail>0` 可展开 |
| 分类 | 6 类标签正确映射；SSO 失效/限流/网络/永久/bot/未知 |
| ops 日志 | `web_account_build_conversion_failed` 含 `class` 字段 |
| 回归 | `TestClassifyConversionError` 通过；旧前端不解析 `class` 仍可用 |

### 15.9 代码锚点

| 主题 | 路径 |
|------|------|
| 错误产生 | `infra/provider/web/sso_build.go` `sso2oauth[phase]`、`conversionHTTPError`、`ClassifyConversionError` |
| 收集 | `application/account/service.go:2067-2094` `convertWebAccountsToBuild` |
| 重试日志（含 class 范式） | `application/account/service.go:2172` |
| HTTP 响应 | `transport/http/account/handler.go` `buildConversionResponse`、`newBuildConversionResponse`、`streamWebToBuildConversion`、`executeWebToBuildConversionTask` |
| Task registry | `application/admintask/registry.go` `Task.Fail`/`Finish`/`ListRecent` |
| tasks 端点 | `transport/http/admintask/handler.go` `/tasks/recent`（已存在） |
| FE dock | `frontend/.../admin-task-dock.tsx` |
| FE 页面 | `frontend/.../accounts-page.tsx` `buildConversionFailureMessage`、convertWebAccounts mutation |
| FE API | `frontend/.../accounts-api.ts` `BuildConversionFailureDTO`、`decodeBuildConversionFailures`、`listActiveAdminTasks` |
| i18n | `frontend/src/shared/i18n/index.ts` `apiErrors.accountConversionFailed`、`accounts.conversionCompleted` |

### 15.10 修复顺序

> **原则：** 后端 class 贯穿先行（同文件串行）；前端类型解码随后；弹窗与 dock 改造可并行。每步带验证。

```
步骤1  后端 class 贯穿（service+HTTP 响应）
       ├─ B1  service.go 收集处填 Class
       ├─ B2  service.go 失败日志加 class
       ├─ B6  handler.go newBuildConversionResponse 传递 Class
       └─ T2  conversion_test.go 补 Class 期望
       验证：go test ./internal/application/account/ ./internal/infra/provider/web/

步骤2  sync 整体失败补 err 细节
       └─ B3  handler.go:1083 区分 ctx cancel / 写 err.Error()
       验证：go build ./internal/transport/http/account/（无新增测试，逻辑简单）

步骤3  async 整体失败保留 Failures
       ├─ B4  registry.go 新增 FailWithResult
       ├─ T3  registry_test.go 覆盖
       └─ B5  handler.go executeWebToBuildConversionTask 调用
       验证：go test ./internal/application/admintask/ ./internal/transport/http/account/

步骤4  前端类型与解码（可与步骤1-3 并行）
       └─ F1  accounts-api.ts DTO+decoder+fromRecord 传递 class
       验证：npm run typecheck

步骤5  结果弹窗（依赖 F1）
       ├─ F7  accounts-page.tsx Dialog + state
       ├─ F8  保留 toast 简略
       └─ F9  i18n 中英双语
       验证：npm run typecheck && npm run lint

步骤6  dock 改造（依赖 F1，可与步骤5 并行）
       ├─ F2  accounts-api.ts listRecentAdminTasks
       ├─ F3  accounts-api.ts 导出 decoder
       ├─ F4  admin-task-dock.tsx 数据源切换
       ├─ F5  admin-task-dock.tsx refetch 降频
       └─ F6  admin-task-dock.tsx TaskRow 展开 failures
       验证：npm run typecheck && npm run lint

收尾   全量验证
       ├─ go test ./...（后端回归）
       ├─ npm run typecheck && npm run lint（前端回归）
       └─ 手测：单号失败/批量失败/整体失败/取消 各场景
```

**依赖关系：**
- 步骤1→2→3 后端同文件（service/handler/registry）建议串行，避免合并冲突
- 步骤4 依赖步骤1 的 class 契约，但可并行开发（类型先行）
- 步骤5、6 均依赖步骤4，彼此可并行

**并行建议：** 步骤5（弹窗）与步骤6（dock）改动不同文件，可由不同执行者并行；步骤1-3 后端建议同一执行者串行以避免 handler.go 冲突。

---

## R8 双池逻辑库存（远期 RFC）

本期不表合并；Web/Console 双行 + R3h 止血已交付。远期候选：D1 主行+投影行 / D2 单 identity 多 channel（见 `号池调度.md`），需独立 RFC，不在本文展开。

---

## 附录 A — 基线（勿回滚）

| 项 | 状态 |
|----|------|
| Q1–Q9 DefaultEgress / 导入 Console / trusted / CLI 筛 / settings.cli | 完成 |
| CLI P1–P10 分层水位 | 完成 |
| C0 OIDC | 完成 |

---

## 一句话

**剩余工作聚焦 §15 SSO2OAUTH 错误展示强化：后端 class 贯穿 + sync/async 整体失败补细节 + 前端弹窗与 dock 改造，分 6 步可并行；R8 双池合并留远期。**
