# origin 修改计划（OIDC / SSO→Build 行为复刻）

> **读者：** 任意编码模型 / 人类执行者。  
> **属性要求：** **完整、通用、明确、无副作用**。  
> **产品本质：** 号池调度（选号 → 调用 → 即时反馈）；OpenAI/Anthropic 是外壳。  
> **本版重点：** 以 **`抓包分析.md` + 0.2.106 真机抓包 + `grok-build` 源码 + `sso2oauth.py` 顺序** 为证据，重写 **SSO→OAuth Convert / OIDC form / JWT 关联 / Refresh / Enrichment** 的执行计划。  
> **证据 SSOT（事实层）：** 同仓 `抓包分析.md`（2026-07-22，二进制 `grok 0.2.106`）。  
> **工程 SSOT（任务层）：** 本文。  
> **基线分支：** `dev`（pool 任务 H1 旧结论已作废；见 §0.3）。  

---

## 0. 质量标尺与阅读约定

### 0.1 四条标尺

| 标尺 | 含义 | 检查问句 |
|------|------|----------|
| **完整** | 请求头 → body → 响应字段 → JWT 解码 → 落库/后续 API 字段全部串联 | 漏了「谁写、谁读、谁清、谁测」吗？ |
| **通用** | 端点类枚举 + 共享 builder；不散落两套 form 魔法字符串 | 另一条 OAuth 路径会不会又分叉？ |
| **明确** | URL、头、body、期望状态码、JWT claim→字段映射写死 | 另一个模型能否不猜直接改？ |
| **无副作用** | 不改 sticky/视频钉号/egress 防重放；日志不落 token；默认不换推理指纹 | 会不会误 bot 标记、误杀号、双生成？ |

### 0.2 两条链路（禁止混头）

| 链路 | 用途 | 本仓落点 | 抓包覆盖 |
|------|------|----------|----------|
| **A. 官方 CLI 登录/运行** | 人浏览器同意 + device/token + refresh + `/v1/responses` | `cli/oauth.go`、`cli/adapter.applyHeaders` | CLI 侧全覆盖；浏览器同意页 **不可见** |
| **B. 号池 Convert** | SSO cookie 自动 verify/approve + 同上 device/token → 落 Build 凭证 | `web/sso_build.go` + account convert | 脚本 `sso2oauth.py` 为顺序骨架；**浏览器步待专用补抓** |

### 0.3 已否定的旧计划结论（禁止写回代码）

| 旧说 | 证据（0.2.106） |
|------|----------------|
| auth.x.ai **表单**应全站浏览器 UA + Sec-Fetch | **否** — device/token/refresh 均为 **CLI 双 UA**，无 Origin/Referer/Sec-Fetch |
| login-config 可用 `curl/7.80.0` 裸检 | **否** — 官方仅 CLI 头 |
| Token 交换必须挂 `X-XAI-Token-Auth` | **否** — oauth **form 无此头**；仅 **cli-chat-proxy API** 有 |
| 旧 H1「OAuthForm = 浏览器」 | **作废** — 见 `抓包分析.md` §1.2 / §4.3 |

### 0.4 号池特征（策略默认）

| 特征 | 设计默认 |
|------|----------|
| 池内多为免费号 | Convert 成功率 + **换号** 优先；不在免费模型上堆玄学冷却 |
| 付费极少 429 | 同一套 class；额外只守写出不重放 / sticky / 凭证真死 |
| Convert 指纹错 = SSO 可能被 bot 标 | **Form 必须像 CLI；HTML 必须像浏览器**；共享 session cookie 连续 |
| 脏 token 进池 = 全链路污染 | token 后 **解码 JWT**，`bot_flag` / 身份字段关联 enrichment 与 refresh |

### 0.5 不变量（禁止破坏）

1. 传输超时 → 不 `MarkFailure` 长冷却。  
2. sticky / previous_response → chat attempts=1。  
3. 视频 generate **钉号**；禁止生成阶段换号。  
4. egress：`WroteRequest` 后禁止重放 POST。  
5. 日志/审计不落 cookie、refresh_token、完整上游 body（可用 jsonshape 键树）。  
6. Convert/refresh 继续用现有分布式锁与 scheduler，**不用**进程内全局互斥替代 Redis。  
7. **不做** 整包 PKCE；**不做** 每请求 set-birth-date（W1 已定 AdultPending 一次 ensure）。  

### 0.6 已完成的池子任务（本文不再展开）

`T2` 导入 429、`W1` 年龄门、`T1+T1b` no_image/生图换号、`T3` 非流动态超时、`T4` class 冷却、`T5` reauth 原因、`H2` CLI 推理头可配、`T6` jitter、`T7` shape/out_bytes、`T8` 路由 debug、`O1` convert class、`O2` refresh enqueue。  

**T9** CLI/Console 非流 SSE = **明确不做**。  

**本文任务全集 = C0 族（OIDC 安全复刻）**。

---

## 1. 常量与身份头矩阵（实现必须对齐）

### 1.1 常量（抓包 + Rust 一致）

| 常量 | 值 |
|------|-----|
| `client_id` | `b1a00492-073a-47ea-816f-4c329264a828` |
| CLI 版本（目标） | `0.2.106`（配置化；脚本现状 `0.2.101` 待升） |
| CLI UA | `grok-pager/{ver} grok-shell/{ver} (linux; aarch64)` |
| Device | `POST https://auth.x.ai/oauth2/device/code` |
| Token | `POST https://auth.x.ai/oauth2/token`（**不是** `/oauth/token`） |
| Verify | `POST https://auth.x.ai/oauth2/device/verify` |
| Approve | `POST https://auth.x.ai/oauth2/device/approve` |
| Discovery | `GET https://auth.x.ai/.well-known/openid-configuration` |
| Login-config | `GET https://cli-chat-proxy.grok.com/v1/login-config` |
| Stable | `GET https://x.ai/cli/stable` |
| API Token-Auth | `xai-grok-cli`（**仅** cli-chat-proxy） |
| Device `referrer` 字段 | `grok-build` |
| Device surface | `ui`（Convert 自动同意仍建议 `ui`；勿默认 `headless`） |

**默认 scope（0.2.106 body 实测）：**

```text
openid profile email offline_access
grok-cli:access api:access
conversations:read conversations:write
workspaces:read workspaces:write
```

### 1.2 端点类枚举（Go 必须落成共享 builder）

| 类名 | 适用 | UA | 关键头 | 禁止 |
|------|------|-----|--------|------|
| **CLIAuthForm** | device/code、token(device_code)、token(refresh) | CLI 双 UA | `Content-Type: application/x-www-form-urlencoded`；device/poll 加 `x-grok-client-version` + `x-grok-client-surface: ui` | 浏览器 UA；Origin/Referer/Sec-Fetch；oauth form 上的 `X-XAI-Token-Auth` |
| **BrowserAuthHTML** | verify 页 GET、verify POST、consent GET、approve POST | 浏览器 UA（如 Chrome Mobile） | `Accept: text/html...`；`Cookie: sso=…; sso-rw=…`；逐步 Referer/Origin | CLI 双 UA；Token-Auth |
| **CLIApiMeta** | login-config / user / settings / models / billing | CLI 双 UA | Bearer（除 login-config）+ `x-xai-token-auth` + version + mode | curl UA |
| **CLIInference** | `POST /v1/responses` | CLI 双 UA | Token-Auth + mode + **identifier=`grok-pager`** + session 族 | 把 settings 的 `grok-shell` 硬套到 responses |
| **CLIProbe** | `GET x.ai/cli/stable` | 抓包可无 UA | `Accept: */*` | 强行 curl 或乱塞 form 头 |
| **CLIShared** | discovery 等 shared client | CLI 双 UA | `Accept: */*` | — |

**Python 对照（低 bot 路径）：**

- Form → `build_oidc_form_headers`  
- HTML → `build_oidc_browser_headers(step)`  
- login-config → `build_login_config_headers`  
- init → `build_user_headers` / `build_enrichment_headers`  
- responses → `build_responses_headers`  

### 1.3 当前 origin 错误态（必须改）

| 文件 | 现状 | 问题 |
|------|------|------|
| `cli/oauth.go` `applyOAuthFormHeaders` | 浏览器 UA + Sec-Fetch + Origin/Referer；token 加 Token-Auth | **与抓包相反** |
| `cli/oauth.go` scope | 无 conversations/workspaces；无 `referrer` | 字段不全 |
| `cli/oauth.go` refresh | 无 `principal_type/id` | 与抓包 refresh body 不一致 |
| `web/sso_build.go` `applySSOBuildRequestHeaders` | **整链**浏览器头；token 也 Token-Auth | device/token 应用 CLIAuthForm |
| `web/sso_build.go` device body | scope 到 conversations；**无 referrer/workspaces** | 落后 0.2.106 |
| `web/sso_build.go` seed | 解码 sub/email/team；**不读 bot_flag；不做 CLI init** | 关联断裂 |
| 两套 OAuth | cli 与 sso_build 各写一套 | 应共享 form/browser builder |

---

## 2. 数据关联图（JWT / 字段不是随便填）

> 原则：**上游返回什么 → 解码什么 → 下游请求带什么**。禁止硬编码无关 uuid/email。

### 2.1 字段血缘

```text
SSO JWT (cookie value)
  ├─ payload.session_id ──► agent_id = uuid5(NAMESPACE_URL, "grok2api:grok-cli-agent:"+session_id)
  │                         （无 session_id 时用 sha256(sso) 作 seed）
  ├─ payload 可选 bot_flag  仅观测；Convert 是否继续以 SSO 探活/后续 access 为准
  └─ cookie: sso=<jwt>; sso-rw=<jwt>  ──► 仅 BrowserAuthHTML 步

POST device/code (CLIAuthForm)
  body: client_id, scope(full), referrer=grok-build
  resp: device_code, user_code, verification_uri(_complete), interval, expires_in
        │
        ├─ user_code ──────────────► verify/approve form
        └─ device_code ────────────► token poll form

Browser: GET verification_uri_complete → POST verify{user_code}
       → GET consent → POST approve{user_code, action=allow, principal_type=User, principal_id=""}
  （principal_id 在 approve 时常为空字符串；勿填假 uuid）

POST token device_code grant (CLIAuthForm)
  resp JSON:
    access_token  ──JWT decode──► at_claims
    refresh_token ──────────────► 加密落库 EncryptedRefreshToken
    id_token      ──JWT decode──► id_claims
    expires_in    ──────────────► ExpiresAt = now + expires_in
    token_type, scope (若有)

at_claims / id_claims 映射（优先级写死）：

| 落库 / 后续 | 来源优先序 | 用途 |
|-------------|------------|------|
| UserID / principal_id | `at.sub` → `/v1/user`.userId | refresh body `principal_id`；x-userid |
| Email | `id.email` → `at.email` → `/v1/user`.email | 账号名；x-email |
| TeamID | `at.team_id` → `/v1/user`.teamId | 元数据 |
| OIDCClientID | 常量 client_id（或 at.aud 若为该 uuid） | 刷新仍用同一 client |
| bot_flag 原始值 | **必须** `at.bot_flag_source`（access_token） | 污染判定 + 标签；见 §2.2 |
| scope 快照 | token JSON.scope 或 at.scope | 观测；不改请求权限模型 |
| issuer | at.iss | 观测 |

GET /v1/user (CLIApiMeta, Bearer=access_token)
  覆盖/补全 userId、email、team；校验 token 可用
  ──► enrichment 头：x-userid / x-email 用**最终** userId/email

GET /v1/settings|models|billing… (CLIApiMeta + enrichment)
  headers 依赖上一行的 userId/email；settings 常见 identifier=grok-shell

运行时 refresh (CLIAuthForm, 热路径)
  body: grant_type=refresh_token, refresh_token, client_id,
        principal_type=User, principal_id=<库内 UserID>
  （UserID 未知时可省略 principal_*，兼容旧号；有则必带）

运行时 /v1/responses (CLIInference)
  Authorization: Bearer <access>
  x-xai-token-auth: xai-grok-cli
  identifier 样例 grok-pager（与 settings 的 grok-shell 不同）
```

### 2.2 `bot_flag_source` 双语义（禁止混用）

| 场景 | 判定 | 行为 |
|------|------|------|
| **Convert 污染门闩**（sso2oauth / 抓包分析 BOT_OK） | access JWT：`bot_flag_source` **缺失** 或 **`NP`**（字符串）→ 干净；其它字符串/非 NP → **污染** | 打标 `bot_<value>`；**默认拒绝进入可用池**或强制 `BuildBotFlagged` 运营可见；连续污染可熔断批转换 |
| **运行时 Super/XAI 路由**（origin 既有） | access JWT：`bot_flag_source` 为 **JSON number `1`** → `CredentialMetadata.BuildBotFlagged=true` | 仅影响 Super/XAI 视频等路由；**不是**「Convert 放行」条件 |

实现要求：

1. Convert 后 **解码 access_token**（不要只解 id_token）。  
2. 持久化/标签记录 **原始 claim 字符串化**（便于对账），同时按上表两套规则分别计算。  
3. 日志只打 `bot_flag_present` / `bot_flag_class=clean|contaminated|super_signal`，**不打 token**。  

### 2.3 响应期望（实现断言用）

| 步骤 | 期望 HTTP | 关键响应 |
|------|-----------|----------|
| device/code | 200 | JSON 含 device_code/user_code/verification_uri_complete；interval 默认 5；expires_in≈1800 |
| device/code | 429 | **退避**，禁止狂刷 |
| verify POST | 302/303 或 2xx 且最终 URL 含 consent（Go 现逻辑） | Location → consent |
| approve POST | 302/303；最终 URL 含 done | — |
| token poll | 200 + access_token | 或 `error=authorization_pending/slow_down/expired_token/access_denied` |
| refresh | 200 + access_token | 4xx invalid_grant → Permanent |
| /v1/user | 200 | userId/email 与 JWT 交叉验证 |

---

## 3. 按 sso2oauth 阶段的复刻规格

阶段号与 `~/grok2api/scripts/sso2oauth.py` 的 `phase_header` **一一对应**。  
每节：**顺序 → 请求 → 期望 → 数据写入 → origin 落点**。

### Phase 00 — SSO JWT Decode

| 项 | 规格 |
|----|------|
| 动作 | base64url 解 SSO JWT header/payload；**不校验签**（号池侧与脚本一致） |
| 派生 | `agent_id` ← `session_id` 或 token hash（§2.1） |
| 写入 | 内存 flow_state；可选观测 SSO 是否自带 bot_flag |
| 落点 | `normalizeSSOToken` 旁；Convert 入口 |
| 非目标 | 不把 SSO payload 当 Build access |

### Phase 00a — Rate Limits（可选）

| 项 | 规格 |
|----|------|
| 请求 | `POST https://grok.com/rest/rate-limits` JSON `{requestKind, modelName}` |
| 头 | **浏览器** UA + `cookie: sso=`；**不是** CLI 序 |
| 抓包 | 官方 CLI **不做**；Convert 健康检查可选 |
| 落点 | 可选预检；失败 class 可映射 `rate_limited` / `sso_dead` |
| 非目标 | 不作为 device 前置硬依赖（除非配置 strict） |

### Phase 01 — Version `GET https://x.ai/cli/stable`

| 项 | 规格 |
|----|------|
| 头 | `Accept: */*`；`Accept-Encoding: gzip, br, deflate`；抓包 **可无 UA** |
| 响应 | body 版本串（如 `0.2.106`）；可能 Set-Cookie `__cf_bm` |
| 落点 | Convert 可选预检；版本可驱动 CLI UA 配置 |
| 非目标 | 不塞 form 头 |

### Phase 02 — Login Config `GET /v1/login-config`

| 项 | 规格 |
|----|------|
| 头 | **CLIApiMeta 未登录**：双 CLI UA + `x-grok-client-version` + `x-grok-client-mode: interactive` + `x-grok-client-identifier: grok-shell` + 可选 `x-grok-agent-id` |
| 禁止 | `User-Agent: curl/7.80.0` |
| 响应 | `device_flow` 等；仅观测 |
| 落点 | Convert 可选预检（P1） |

### Phase 03 — OIDC Discovery

| 项 | 规格 |
|----|------|
| 头 | CLIShared：双 CLI UA + Accept `*/*` |
| 响应 | 校验 `device_authorization_endpoint` / `token_endpoint` 与常量一致即可 |
| 落点 | 可选；失败不阻断若硬编码端点仍可用 |

### Phase 04 — Device Code（**P0**）

**请求头（CLIAuthForm）：**

```http
POST /oauth2/device/code HTTP/1.1
Host: auth.x.ai
Accept: */*
Accept-Encoding: gzip, br, deflate
Content-Type: application/x-www-form-urlencoded
User-Agent: grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
x-grok-client-surface: ui
x-grok-client-version: 0.2.106
```

**Body：**

```text
client_id=b1a00492-073a-47ea-816f-4c329264a828
scope=openid profile email offline_access grok-cli:access api:access conversations:read conversations:write workspaces:read workspaces:write
referrer=grok-build
```

**响应 → 状态：** `device_code`、`user_code`、`verification_uri_complete`、`interval`、`expires_in`。  
**落点：** 共享 `CLIAuthForm` builder；`cli/oauth.startDevice` + `sso_build` device 共用。  
**429：** 批转换退避（O1 class 已有 rate_limited）。

### Phase 04a–04c 前半 — Verify / Consent / Approve（**BrowserAuthHTML**）

> 本批 traffic **不可见**；以 sso2oauth + Python `build_oidc_browser_headers` 为准，待补抓替换。

| 步 | 方法/URL | 头要点 | Body |
|----|----------|--------|------|
| 04a | GET `verification_uri_complete` | 浏览器 UA；Accept HTML；Referer `accounts…/oauth2/device`；Cookie sso | — |
| 04b | POST `/oauth2/device/verify` | 浏览器；Origin accounts；Referer 带 user_code | `user_code` |
| 04b-2 | GET Location(consent) | 浏览器；Referer verify | — |
| 04c | POST `/oauth2/device/approve` | 浏览器；Origin accounts；Referer=consent URL | `user_code`、`action=allow`、`principal_type=User`、`principal_id=`（空） |

**Cookie：** 同一会话连续；`Set-Cookie` 合并进 jar（origin `captureCookies` 已有）。  
**落点：** **仅** `sso_build.do` 的 HTML 步用 Browser builder；**不要**套到 device/token。  
**期望：** verify/approve 3xx 或最终 URL 语义（consent/done）与现 Go 校验兼容。

### Phase 04c 后半 — Token Poll（**P0**）

**请求头（CLIAuthForm，与 device 同族）：**

```http
POST /oauth2/token HTTP/1.1
Host: auth.x.ai
Accept: */*
Accept-Encoding: gzip, br, deflate
Content-Type: application/x-www-form-urlencoded
User-Agent: grok-pager/0.2.106 grok-shell/0.2.106 (linux; aarch64)
x-grok-client-surface: ui
x-grok-client-version: 0.2.106
```

**Body：**

```text
grant_type=urn:ietf:params:oauth:grant-type:device_code
device_code=<from phase 04>
client_id=b1a00492-073a-47ea-816f-4c329264a828
```

**成功后强制管线：**

1. 解析 `access_token` / `refresh_token` / `id_token` / `expires_in`。  
2. `decodeJWT(access_token)` → bot_flag / sub / team_id / scope / iss。  
3. `decodeJWT(id_token)` → email（优先）。  
4. 污染判定 §2.2 → 拒绝或打标（产品默认：**污染不进入可调度池**）。  
5. 组装 `CredentialSeed`（UserID/Email/TeamID/OIDCClientID/ExpiresAt/…）。  

**错误：** `authorization_pending` 继续；`slow_down` 加大 interval；`access_denied`/`expired_token` 永久失败。  
**落点：** `sso_build.pollToken` + 抽公共 `exchange`；`cli/oauth.exchange` 对齐头。

### Phase 04 旁路 — Refresh（热路径 **P0**）

**请求头：** CLI 双 UA；Content-Type form；Accept `*/*`。  
本批抓包 **未见** surface/version 与 Token-Auth → **不要**为 refresh 强制 Token-Auth。  
可选：与 device 一致带 version/surface（若加，单测锁定；默认按抓包最小集）。

**Body：**

```text
grant_type=refresh_token
refresh_token=<opaque>
client_id=b1a00492-073a-47ea-816f-4c329264a828
principal_type=User
principal_id=<credential.UserID>
```

**落点：** `cli/oauth.refresh` 签名扩展为可传入 `principalID`；adapter 从 `credential.UserID` 注入。  
**成功后：** 可再解 access JWT 更新 bot 元数据（若 claim 变化）；写回加密 token + ExpiresAt（既有 refresh 路径）。

### Phase 05 — CLI Init / Enrichment（**P1 最小 / P2 全套**）

**最小（Convert 成功后建议必做）：**

```http
GET /v1/user
Authorization: Bearer <access_token>
User-Agent: grok-pager/… grok-shell/…
x-grok-client-version: 0.2.106
x-grok-client-mode: interactive
x-xai-token-auth: xai-grok-cli
Accept: */*
```

- 用响应 **覆盖** seed.UserID / Email / TeamID（§2.1 优先序）。  
- 401 → token 不可用，Convert 失败。  

**扩展 enrichment（可选，对齐 sso2oauth 05b–05g）：**

| 端点 | 额外头 |
|------|--------|
| `/v1/settings` | `x-userid`、`x-email`、`x-grok-client-identifier: grok-shell` |
| `/v1/models` | 同上 enrichment |
| `/v1/billing?format=credits` | 同上 + userid |
| `/v1/user?include=subscription` | 基础 auth_h |

**关联：** enrichment 的 userid/email **必须**来自 JWT 或 `/v1/user`，禁止空串乱填。  
**污染 token：** sso2oauth 可 skip preflight；origin 若已拒绝入池则不必 init。

### Phase 06 / 99 — 预检额外 / 落库

| 项 | 规格 |
|----|------|
| 标签 | `converted`；污染则 `bot_<flag>` |
| 加密 | access/refresh 走既有 cipher |
| SourceKey | 保持 `sso-build:` + hash(access) 或既有策略 |
| 日志 | class、status、bot_flag_class、email hash/前缀；无 raw token |

---

## 4. 任务拆分与执行顺序

### 4.1 任务总表（C0 族）

| 序 | ID | 任务 | 优先 | 主落点 |
|----|----|------|------|--------|
| 1 | **C0-1** | 共享身份 builder：`CLIAuthForm` / `BrowserAuthHTML` / 常量 scope+referrer+UA | P0 | 新建小包或 `provider/cli` 导出；两路径引用 |
| 2 | **C0-2** | `cli/oauth.go`：device/token/refresh 改 CLIAuthForm；scope+referrer；refresh principal_*；去掉错误浏览器头与 form Token-Auth | P0 | `oauth.go` + `oauth_test.go` |
| 3 | **C0-3** | `web/sso_build.go`：device/token 用 CLIAuthForm；HTML 步用 BrowserAuthHTML；body 对齐；Cookie 仅 HTML | P0 | `sso_build.go` + 测试 |
| 4 | **C0-4** | Token 后 JWT 管线：claims→seed；Convert 污染门闩；与既有 numeric `BuildBotFlagged` 并存文档化 | P0 | `sso_build` + account convert 接线 |
| 5 | **C0-5** | Convert 后最小 CLI init：`GET /v1/user` 交叉填充 UserID/Email/Team | P1 | `sso_build` 或 application convert |
| 6 | **C0-6** | Refresh 注入 principal_id；可观测 refresh class | P0/P1 | `adapter.RefreshCredential` → oauth |
| 7 | **C0-7** | 可选预检：stable + login-config（CLI 头，无 curl）；device 429 退避已有 class 复用 | P1 | convert 编排 |
| 8 | **C0-8** | （可选）全套 enrichment；agent_id 稳定派生 | P2 | application |
| 9 | **C0-9** | （可选）升级 `~/grok2api/scripts/sso2oauth.py` 至 0.2.106 + workspaces；删 curl login-config | P2 | 脚本仓 |
| — | — | 补抓 Browser 步 → 回写 `抓包分析.md` | 运维 | 非代码阻断 |

### 4.2 执行顺序（复制即用）

```text
1  C0-1  共享 builder + 常量（scope/referrer/UA/version 配置入口）
2  C0-2  cli/oauth.go 表单与 refresh（热路径，单测锁头/body）
3  C0-3  sso_build.go 双轨头 + device body
4  C0-4  JWT 解码关联 + Convert 污染门闩
5  C0-6  refresh principal_*（可与 2 同 PR）
6  C0-5  Convert 后 GET /v1/user 最小 enrichment
7  C0-7  可选预检与 429 策略收口
8  C0-8/C0-9  可选全量 init / 脚本对齐
（禁止）整包 PKCE；HTML 步改 CLI UA；form 步改浏览器 UA；oauth form 强制 Token-Auth
（禁止）把 Super numeric bot_flag=1 与 Convert 污染判定混成一个 if
```

### 4.3 每任务无副作用清单

| 风险 | 防护 |
|------|------|
| 指纹突变导致旧号 refresh 失败 | refresh 在无 UserID 时省略 principal_*；单测两种 body |
| 误拒干净号 | 仅 access 上明确非 NP 字符串才 contaminated；缺失/NP 放行 |
| Super 路由回归 | `BuildBotFlagged` 仍只认 numeric `1`；单测保留 |
| 日志泄密 | 禁止打印 access/refresh/sso；测试用 fake JWT |
| 双实现再分叉 | C0-1 先合并 builder，禁止再在两文件复制 UA 字符串 |
| 浏览器步误伤 | Cookie/浏览器 UA **仅** accounts HTML 导航 |
| 多实例 | 不改 lock key 语义 |

### 4.4 验收通式

```text
go test ./backend/internal/infra/provider/cli/...
go test ./backend/internal/infra/provider/web/...
go test ./backend/internal/application/account/...
```

单测 table 至少覆盖：

- CLIAuthForm：UA 含子串 `grok-pager/` 与 `grok-shell/`；**无** `Sec-Fetch-`；**无** `X-XAI-Token-Auth` on token。  
- device body：含 `referrer=grok-build` 与 `workspaces:read`。  
- Browser 步：UA 为浏览器；带 Cookie。  
- JWT：sub/email/team/bot 映射；污染拒绝路径。  
- refresh body：有 UserID 时含 principal_*。  

---

## 5. 移植后处理思路（实现备忘）

### 5.1 推荐包结构（最小侵入）

```text
backend/internal/infra/provider/xaiauth/   # 或 cli/authheaders.go
  constants.go      client_id, default scope, URLs
  headers.go        ApplyCLIAuthForm(req, version, surface)
                    ApplyBrowserAuthHTML(req, step, userCode, referer, ua)
  jwt.go            DecodeClaims(token) map
  botflag.go        ClassifyConvertBot(claims) / 说明与 Super numeric 关系
```

`cli/oauth.go` 与 `web/sso_build.go` **只编排**，不各自拼 UA。

### 5.2 Convert 成功路径伪代码

```text
sso := normalize(decrypt(cred))
jar := {sso, sso-rw}
// optional: stable, login-config (CLIApiMeta)
device := POST device/code (CLIAuthForm, full scope+referrer)
GET verification_uri_complete (Browser+jar)
POST verify (Browser+jar)
POST approve (Browser+jar)
tok := poll token (CLIAuthForm)
claims := decode(tok.access)
if contaminated(claims): tag+return error class permanent/bot
seed := seedFrom(tok, claims, id_claims)
user := GET /v1/user (CLIApiMeta, Bearer access)  // 覆盖 identity
return seed
```

### 5.3 与 Python 生产路径对齐点

| 点 | Python | origin 目标 |
|----|--------|-------------|
| Form 头 | `build_oidc_form_headers` | CLIAuthForm |
| Browser 头 | `build_oidc_browser_headers(step)` | BrowserAuthHTML |
| referrer | `grok-build` | 同 |
| token→cred | `_token_response_to_cred` | seed + bot 门闩 |
| enrichment | `enrich_cli_token_async` | 最小 /v1/user；全套可选 |
| TLS 指纹 | curl_cffi impersonate | **本期不强制**（egress 既有）；记录为后续 |

### 5.4 配置入口（建议）

| 键 | 默认 | 说明 |
|----|------|------|
| `provider.build.cliVersion` | `0.2.106` | 驱动 UA/version 头 |
| `provider.build.convertRejectBot` | `true` | Convert 污染拒绝 |
| `provider.build.convertInitUser` | `true` | Convert 后 GET /v1/user |
| `provider.build.clientMode` | 保持现状（headless/interactive 可配） | **推理**头；与 OIDC form surface 分离 |

---

## 6. 不做清单

- 整包 PKCE / authorize 浏览器流（CLI 主路径是 device）。  
- 把 verify/approve 改成 CLI UA。  
- 把 device/token 改成浏览器 UA 或 Sec-Fetch。  
- oauth form 强制 `X-XAI-Token-Auth`。  
- login-config curl 裸检。  
- 每请求 set-birth-date；视频 generate 换号；流式动态墙钟。  
- 整文件抄 `xai_oidc.py` / 进程全局锁替代 Redis。  
- 默认改变 CLI **推理**指纹（H2 已配置化；C0 不动 responses 除非单独立项）。  

---

## 7. 文档关系

| 文档 | 职责 |
|------|------|
| **`抓包分析.md`** | 事实：抓包头/body、阶段核对、盲区 |
| **本文 `修改计划.md`** | 工程：任务、字段血缘、落点、顺序、验收 |
| `sso2oauth.py` | Convert 可执行顺序骨架（版本待升） |
| `grok_headers.py` / `xai_oidc.py` | Python 生产对照，非直接粘贴源 |
| `grok-build` Rust | scope/referrer/surface 语义 |

---

## 8. 完成度快照

| 区域 | 状态 |
|------|------|
| 池子移植 H1旧/T2/W1/T1/T3–T8/O1/O2 | ✅ 已完成（旧 H1 浏览器 form **结论作废**，由 C0 纠正） |
| **C0-1** 共享 `xaiauth` builder | ✅ |
| **C0-2** `cli/oauth.go` CLIAuthForm + scope/referrer | ✅ |
| **C0-3** `sso_build` 双轨头 + device body | ✅ |
| **C0-4** JWT 血缘 + Convert 污染门闩 | ✅ |
| **C0-5** Convert 后 `GET /v1/user` 最小 enrichment | ✅（失败不阻断，JWT 回落） |
| **C0-6** refresh `principal_*` | ✅ |
| **C0-7a** device 429 短退避 | ✅ |
| **C0-7b** soft preflight（stable + login-config，fail-open） | ✅ |
| **C0-7c** BuildClientVersion 从 provider.build 注入 OIDC | ✅ |
| C0-8 / C0-9 / Browser 补抓 | ⬜ 可选 |

---

*本版依据：`抓包分析.md`（0.2.106）、`sso2oauth.py` 阶段序、Python `grok_headers`/`xai_oidc`、origin `cli/oauth.go` + `web/sso_build.go` 现状。浏览器 OIDC 步标注脚本骨架，补抓后只改 `抓包分析.md` 对应节与 Browser builder 细节，不推翻 Form=CLI 主结论。*
